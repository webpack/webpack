/*
	MIT License http://www.opensource.org/licenses/mit-license.php
	Author Raj Aryan (based on SWC parser by Alexander Akait)
*/

"use strict";

// cspell:ignore apos notpre

// #region html entities
// The contents of this region are auto-generated by
// `tooling/generate-html-entities.js` from `tooling/html-entities.json`.
// Do not edit by hand — re-run the generator (via `yarn fix:special`) to refresh.
//
// Delta-encoded WHATWG named character references. Each record in
// HTML_ENTITY_SOURCE is `<sharedByte><suffix>\t<valueLenByte><value>`:
//   - sharedByte    = String.fromCharCode(0x20 + n) where n is the count
//                     of leading chars shared with the previous record's
//                     name (range 0x20..0x40).
//   - suffix        = the rest of this record's name.
//   - valueLenByte  = String.fromCharCode(0x30 + len) where len is the
//                     UTF-16 code-unit length of the value (1 or 2).
//   - value         = `len` UTF-16 code units of the decoded characters.
// Names are sorted alphabetically so adjacent entries share long prefixes.
// prettier-ignore
// cspell:disable-next-line
const HTML_ENTITY_SOURCE = " AElig\t1Æ%;\t1Æ!MP\t1&#;\t1&!acute\t1Á&;\t1Á!breve;\t1Ă!circ\t1Â%;\t1Â\"y;\t1А!fr;\t2𝔄!grave\t1À&;\t1À!lpha;\t1Α!macr;\t1Ā!nd;\t1⩓!ogon;\t1Ą\"pf;\t2𝔸!pplyFunction;\t1⁡!ring\t1Å%;\t1Å!scr;\t2𝒜\"sign;\t1≔!tilde\t1Ã&;\t1Ã!uml\t1Ä$;\t1Ä Backslash;\t1∖\"rv;\t1⫧#wed;\t1⌆!cy;\t1Б!ecause;\t1∵\"rnoullis;\t1ℬ\"ta;\t1Β!fr;\t2𝔅!opf;\t2𝔹!reve;\t1˘!scr;\t1ℬ!umpeq;\t1≎ CHcy;\t1Ч!OPY\t1©$;\t1©!acute;\t1Ć\"p;\t1⋒#italDifferentialD;\t1ⅅ\"yleys;\t1ℭ!caron;\t1Č\"edil\t1Ç&;\t1Ç\"irc;\t1Ĉ\"onint;\t1∰!dot;\t1Ċ!edilla;\t1¸\"nterDot;\t1·!fr;\t1ℭ!hi;\t1Χ!ircleDot;\t1⊙&Minus;\t1⊖&Plus;\t1⊕&Times;\t1⊗!lockwiseContourIntegral;\t1∲#seCurlyDoubleQuote;\t1”*Quote;\t1’!olon;\t1∷%e;\t1⩴\"ngruent;\t1≡#int;\t1∯#tourIntegral;\t1∮\"pf;\t1ℂ#roduct;\t1∐\"unterClockwiseContourIntegral;\t1∳!ross;\t1⨯!scr;\t2𝒞!up;\t1⋓#Cap;\t1≍ DD;\t1ⅅ\"otrahd;\t1⤑!Jcy;\t1Ђ!Scy;\t1Ѕ!Zcy;\t1Џ!agger;\t1‡\"rr;\t1↡\"shv;\t1⫤!caron;\t1Ď\"y;\t1Д!el;\t1∇#ta;\t1Δ!fr;\t2𝔇!iacriticalAcute;\t1´+Dot;\t1˙-ubleAcute;\t1˝+Grave;\t1`+Tilde;\t1˜#mond;\t1⋄\"fferentialD;\t1ⅆ!opf;\t2𝔻\"t;\t1¨#Dot;\t1⃜#Equal;\t1≐\"ubleContourIntegral;\t1∯&Dot;\t1¨(wnArrow;\t1⇓&LeftArrow;\t1⇐*RightArrow;\t1⇔*Tee;\t1⫤'ongLeftArrow;\t1⟸.RightArrow;\t1⟺*RightArrow;\t1⟹&RightArrow;\t1⇒+Tee;\t1⊨&UpArrow;\t1⇑(DownArrow;\t1⇕&VerticalBar;\t1∥\"wnArrow;\t1↓)Bar;\t1⤓)UpArrow;\t1⇵$Breve;\t1̑$LeftRightVector;\t1⥐(TeeVector;\t1⥞(Vector;\t1↽.Bar;\t1⥖$RightTeeVector;\t1⥟)Vector;\t1⇁/Bar;\t1⥗$Tee;\t1⊤'Arrow;\t1↧$arrow;\t1⇓!scr;\t2𝒟\"trok;\t1Đ ENG;\t1Ŋ!TH\t1Ð#;\t1Ð!acute\t1É&;\t1É!caron;\t1Ě\"irc\t1Ê%;\t1Ê\"y;\t1Э!dot;\t1Ė!fr;\t2𝔈!grave\t1È&;\t1È!lement;\t1∈!macr;\t1Ē\"ptySmallSquare;\t1◻%VerySmallSquare;\t1▫!ogon;\t1Ę\"pf;\t2𝔼!psilon;\t1Ε!qual;\t1⩵%Tilde;\t1≂#ilibrium;\t1⇌!scr;\t1ℰ\"im;\t1⩳!ta;\t1Η!uml\t1Ë$;\t1Ë!xists;\t1∃\"ponentialE;\t1ⅇ Fcy;\t1Ф!fr;\t2𝔉!illedSmallSquare;\t1◼&VerySmallSquare;\t1▪!opf;\t2𝔽\"rAll;\t1∀\"uriertrf;\t1ℱ!scr;\t1ℱ GJcy;\t1Ѓ!T\t1>\";\t1>!amma;\t1Γ%d;\t1Ϝ!breve;\t1Ğ!cedil;\t1Ģ\"irc;\t1Ĝ\"y;\t1Г!dot;\t1Ġ!fr;\t2𝔊!g;\t1⋙!opf;\t2𝔾!reaterEqual;\t1≥,Less;\t1⋛'FullEqual;\t1≧'Greater;\t1⪢'Less;\t1≷'SlantEqual;\t1⩾'Tilde;\t1≳!scr;\t2𝒢!t;\t1≫ HARDcy;\t1Ъ!acek;\t1ˇ\"t;\t1^!circ;\t1Ĥ!fr;\t1ℌ!ilbertSpace;\t1ℋ!opf;\t1ℍ\"rizontalLine;\t1─!scr;\t1ℋ\"trok;\t1Ħ!umpDownHump;\t1≎$Equal;\t1≏ IEcy;\t1Е!Jlig;\t1Ĳ!Ocy;\t1Ё!acute\t1Í&;\t1Í!circ\t1Î%;\t1Î\"y;\t1И!dot;\t1İ!fr;\t1ℑ!grave\t1Ì&;\t1Ì!m;\t1ℑ\"acr;\t1Ī#ginaryI;\t1ⅈ\"plies;\t1⇒!nt;\t1∬#egral;\t1∫$rsection;\t1⋂\"visibleComma;\t1⁣)Times;\t1⁢!ogon;\t1Į\"pf;\t2𝕀\"ta;\t1Ι!scr;\t1ℐ!tilde;\t1Ĩ!ukcy;\t1І\"ml\t1Ï$;\t1Ï Jcirc;\t1Ĵ\"y;\t1Й!fr;\t2𝔍!opf;\t2𝕁!scr;\t2𝒥\"ercy;\t1Ј!ukcy;\t1Є KHcy;\t1Х!Jcy;\t1Ќ!appa;\t1Κ!cedil;\t1Ķ\"y;\t1К!fr;\t2𝔎!opf;\t2𝕂!scr;\t2𝒦 LJcy;\t1Љ!T\t1<\";\t1<!acute;\t1Ĺ\"mbda;\t1Λ\"ng;\t1⟪\"placetrf;\t1ℒ\"rr;\t1↞!caron;\t1Ľ\"edil;\t1Ļ\"y;\t1Л!eftAngleBracket;\t1⟨%rrow;\t1←)Bar;\t1⇤)RightArrow;\t1⇆$Ceiling;\t1⌈$DoubleBracket;\t1⟦&wnTeeVector;\t1⥡(Vector;\t1⇃.Bar;\t1⥙$Floor;\t1⌊$RightArrow;\t1↔)Vector;\t1⥎$Tee;\t1⊣'Arrow;\t1↤'Vector;\t1⥚%riangle;\t1⊲,Bar;\t1⧏,Equal;\t1⊴$UpDownVector;\t1⥑&TeeVector;\t1⥠&Vector;\t1↿,Bar;\t1⥘$Vector;\t1↼*Bar;\t1⥒$arrow;\t1⇐$rightarrow;\t1⇔\"ssEqualGreater;\t1⋚$FullEqual;\t1≦$Greater;\t1≶$Less;\t1⪡$SlantEqual;\t1⩽$Tilde;\t1≲!fr;\t2𝔏!l;\t1⋘\"eftarrow;\t1⇚!midot;\t1Ŀ!ongLeftArrow;\t1⟵(RightArrow;\t1⟷$RightArrow;\t1⟶$leftarrow;\t1⟸(rightarrow;\t1⟺$rightarrow;\t1⟹\"pf;\t2𝕃\"werLeftArrow;\t1↙%RightArrow;\t1↘!scr;\t1ℒ\"h;\t1↰\"trok;\t1Ł!t;\t1≪ Map;\t1⤅!cy;\t1М!ediumSpace;\t1 \"llintrf;\t1ℳ!fr;\t2𝔐!inusPlus;\t1∓!opf;\t2𝕄!scr;\t1ℳ!u;\t1Μ NJcy;\t1Њ!acute;\t1Ń!caron;\t1Ň\"edil;\t1Ņ\"y;\t1Н!egativeMediumSpace;\t1​(ThickSpace;\t1​+nSpace;\t1​(VeryThinSpace;\t1​\"stedGreaterGreater;\t1≫&LessLess;\t1≪\"wLine;\t1\n!fr;\t2𝔑!oBreak;\t1⁠\"nBreakingSpace;\t1 \"pf;\t1ℕ\"t;\t1⫬#Congruent;\t1≢$upCap;\t1≭#DoubleVerticalBar;\t1∦#Element;\t1∉$qual;\t1≠(Tilde;\t2≂̸$xists;\t1∄#Greater;\t1≯*Equal;\t1≱*FullEqual;\t2≧̸*Greater;\t2≫̸*Less;\t1≹*SlantEqual;\t2⩾̸*Tilde;\t1≵#HumpDownHump;\t2≎̸'Equal;\t2≏̸#LeftTriangle;\t1⋪/Bar;\t2⧏̸/Equal;\t1⋬%ss;\t1≮'Equal;\t1≰'Greater;\t1≸'Less;\t2≪̸'SlantEqual;\t2⩽̸'Tilde;\t1≴#NestedGreaterGreater;\t2⪢̸)LessLess;\t2⪡̸#Precedes;\t1⊀+Equal;\t2⪯̸+SlantEqual;\t1⋠#ReverseElement;\t1∌$ightTriangle;\t1⋫0Bar;\t2⧐̸0Equal;\t1⋭#SquareSubset;\t2⊏̸/Equal;\t1⋢+perset;\t2⊐̸1Equal;\t1⋣$ubset;\t2⊂⃒)Equal;\t1⊈%cceeds;\t1⊁+Equal;\t2⪰̸+SlantEqual;\t1⋡+Tilde;\t2≿̸%perset;\t2⊃⃒+Equal;\t1⊉#Tilde;\t1≁(Equal;\t1≄(FullEqual;\t1≇(Tilde;\t1≉#VerticalBar;\t1∤!scr;\t2𝒩!tilde\t1Ñ&;\t1Ñ!u;\t1Ν OElig;\t1Œ!acute\t1Ó&;\t1Ó!circ\t1Ô%;\t1Ô\"y;\t1О!dblac;\t1Ő!fr;\t2𝔒!grave\t1Ò&;\t1Ò!macr;\t1Ō\"ega;\t1Ω\"icron;\t1Ο!opf;\t2𝕆!penCurlyDoubleQuote;\t1“)Quote;\t1‘!r;\t1⩔!scr;\t2𝒪\"lash\t1Ø&;\t1Ø!tilde\t1Õ&;\t1Õ#mes;\t1⨷!uml\t1Ö$;\t1Ö!verBar;\t1‾%race;\t1⏞(ket;\t1⎴$Parenthesis;\t1⏜ PartialD;\t1∂!cy;\t1П!fr;\t2𝔓!hi;\t1Φ!i;\t1Π!lusMinus;\t1±!oincareplane;\t1ℌ\"pf;\t1ℙ!r;\t1⪻\"ecedes;\t1≺(Equal;\t1⪯(SlantEqual;\t1≼(Tilde;\t1≾\"ime;\t1″\"oduct;\t1∏#portion;\t1∷*al;\t1∝!scr;\t2𝒫\"i;\t1Ψ QUOT\t1\"$;\t1\"!fr;\t2𝔔!opf;\t1ℚ!scr;\t2𝒬 RBarr;\t1⤐!EG\t1®#;\t1®!acute;\t1Ŕ\"ng;\t1⟫\"rr;\t1↠$tl;\t1⤖!caron;\t1Ř\"edil;\t1Ŗ\"y;\t1Р!e;\t1ℜ\"verseElement;\t1∋(quilibrium;\t1⇋'UpEquilibrium;\t1⥯!fr;\t1ℜ!ho;\t1Ρ!ightAngleBracket;\t1⟩&rrow;\t1→*Bar;\t1⇥*LeftArrow;\t1⇄%Ceiling;\t1⌉%DoubleBracket;\t1⟧'wnTeeVector;\t1⥝)Vector;\t1⇂/Bar;\t1⥕%Floor;\t1⌋%Tee;\t1⊢(Arrow;\t1↦(Vector;\t1⥛&riangle;\t1⊳-Bar;\t1⧐-Equal;\t1⊵%UpDownVector;\t1⥏'TeeVector;\t1⥜'Vector;\t1↾-Bar;\t1⥔%Vector;\t1⇀+Bar;\t1⥓%arrow;\t1⇒!opf;\t1ℝ\"undImplies;\t1⥰!rightarrow;\t1⇛!scr;\t1ℛ\"h;\t1↱!uleDelayed;\t1⧴ SHCHcy;\t1Щ\"cy;\t1Ш!OFTcy;\t1Ь!acute;\t1Ś!c;\t1⪼\"aron;\t1Š\"edil;\t1Ş\"irc;\t1Ŝ\"y;\t1С!fr;\t2𝔖!hortDownArrow;\t1↓%LeftArrow;\t1←%RightArrow;\t1→%UpArrow;\t1↑!igma;\t1Σ!mallCircle;\t1∘!opf;\t2𝕊!qrt;\t1√\"uare;\t1□&Intersection;\t1⊓&Subset;\t1⊏,Equal;\t1⊑(perset;\t1⊐.Equal;\t1⊒&Union;\t1⊔!scr;\t2𝒮!tar;\t1⋆!ub;\t1⋐#set;\t1⋐&Equal;\t1⊆\"cceeds;\t1≻(Equal;\t1⪰(SlantEqual;\t1≽(Tilde;\t1≿#hThat;\t1∋\"m;\t1∑\"p;\t1⋑#erset;\t1⊃(Equal;\t1⊇#set;\t1⋑ THORN\t1Þ%;\t1Þ!RADE;\t1™!SHcy;\t1Ћ\"cy;\t1Ц!ab;\t1\t\"u;\t1Τ!caron;\t1Ť\"edil;\t1Ţ\"y;\t1Т!fr;\t2𝔗!herefore;\t1∴#ta;\t1Θ\"ickSpace;\t2  #nSpace;\t1 !ilde;\t1∼%Equal;\t1≃%FullEqual;\t1≅%Tilde;\t1≈!opf;\t2𝕋!ripleDot;\t1⃛!scr;\t2𝒯\"trok;\t1Ŧ Uacute\t1Ú&;\t1Ú\"rr;\t1↟$ocir;\t1⥉!brcy;\t1Ў#eve;\t1Ŭ!circ\t1Û%;\t1Û\"y;\t1У!dblac;\t1Ű!fr;\t2𝔘!grave\t1Ù&;\t1Ù!macr;\t1Ū!nderBar;\t1_&race;\t1⏟)ket;\t1⎵%Parenthesis;\t1⏝\"ion;\t1⋃%Plus;\t1⊎!ogon;\t1Ų\"pf;\t2𝕌!pArrow;\t1↑'Bar;\t1⤒'DownArrow;\t1⇅\"DownArrow;\t1↕\"Equilibrium;\t1⥮\"Tee;\t1⊥%Arrow;\t1↥\"arrow;\t1⇑\"downarrow;\t1⇕\"perLeftArrow;\t1↖%RightArrow;\t1↗\"si;\t1ϒ$lon;\t1Υ!ring;\t1Ů!scr;\t2𝒰!tilde;\t1Ũ!uml\t1Ü$;\t1Ü VDash;\t1⊫!bar;\t1⫫!cy;\t1В!dash;\t1⊩%l;\t1⫦!ee;\t1⋁\"rbar;\t1‖#t;\t1‖$icalBar;\t1∣(Line;\t1|(Separator;\t1❘(Tilde;\t1≀#yThinSpace;\t1 !fr;\t2𝔙!opf;\t2𝕍!scr;\t2𝒱!vdash;\t1⊪ Wcirc;\t1Ŵ!edge;\t1⋀!fr;\t2𝔚!opf;\t2𝕎!scr;\t2𝒲 Xfr;\t2𝔛!i;\t1Ξ!opf;\t2𝕏!scr;\t2𝒳 YAcy;\t1Я!Icy;\t1Ї!Ucy;\t1Ю!acute\t1Ý&;\t1Ý!circ;\t1Ŷ\"y;\t1Ы!fr;\t2𝔜!opf;\t2𝕐!scr;\t2𝒴!uml;\t1Ÿ ZHcy;\t1Ж!acute;\t1Ź!caron;\t1Ž\"y;\t1З!dot;\t1Ż!eroWidthSpace;\t1​\"ta;\t1Ζ!fr;\t1ℨ!opf;\t1ℤ!scr;\t2𝒵 aacute\t1á&;\t1á!breve;\t1ă!c;\t1∾\"E;\t2∾̳\"d;\t1∿\"irc\t1â%;\t1â\"ute\t1´%;\t1´\"y;\t1а!elig\t1æ%;\t1æ!f;\t1⁡\"r;\t2𝔞!grave\t1à&;\t1à!lefsym;\t1ℵ#ph;\t1ℵ\"pha;\t1α!macr;\t1ā#lg;\t1⨿\"p\t1&#;\t1&!nd;\t1∧#and;\t1⩕#d;\t1⩜#slope;\t1⩘#v;\t1⩚\"g;\t1∠#e;\t1⦤#le;\t1∠#msd;\t1∡&aa;\t1⦨'b;\t1⦩'c;\t1⦪'d;\t1⦫'e;\t1⦬'f;\t1⦭'g;\t1⦮'h;\t1⦯#rt;\t1∟%vb;\t1⊾'d;\t1⦝#sph;\t1∢$t;\t1Å#zarr;\t1⍼!ogon;\t1ą\"pf;\t2𝕒!p;\t1≈\"E;\t1⩰\"acir;\t1⩯\"e;\t1≊\"id;\t1≋\"os;\t1'\"prox;\t1≈&eq;\t1≊!ring\t1å%;\t1å!scr;\t2𝒶\"t;\t1*\"ymp;\t1≈%eq;\t1≍!tilde\t1ã&;\t1ã!uml\t1ä$;\t1ä!wconint;\t1∳\"int;\t1⨑ bNot;\t1⫭!ackcong;\t1≌$epsilon;\t1϶$prime;\t1‵$sim;\t1∽'eq;\t1⋍\"rvee;\t1⊽#wed;\t1⌅&ge;\t1⌅!brk;\t1⎵$tbrk;\t1⎶!cong;\t1≌\"y;\t1б!dquo;\t1„!ecaus;\t1∵&e;\t1∵\"mptyv;\t1⦰\"psi;\t1϶\"rnou;\t1ℬ\"ta;\t1β#h;\t1ℶ#ween;\t1≬!fr;\t2𝔟!igcap;\t1⋂$irc;\t1◯$up;\t1⋃#odot;\t1⨀$plus;\t1⨁$times;\t1⨂#sqcup;\t1⨆$tar;\t1★#triangledown;\t1▽+up;\t1△#uplus;\t1⨄#vee;\t1⋁#wedge;\t1⋀!karow;\t1⤍!lacklozenge;\t1⧫%square;\t1▪%triangle;\t1▴-down;\t1▾-left;\t1◂-right;\t1▸#nk;\t1␣\"k12;\t1▒$4;\t1░#34;\t1▓\"ock;\t1█!ne;\t2=⃥#quiv;\t2≡⃥\"ot;\t1⌐!opf;\t2𝕓\"t;\t1⊥#tom;\t1⊥\"wtie;\t1⋈\"xDL;\t1╗$R;\t1╔$l;\t1╖$r;\t1╓#H;\t1═$D;\t1╦$U;\t1╩$d;\t1╤$u;\t1╧#UL;\t1╝$R;\t1╚$l;\t1╜$r;\t1╙#V;\t1║$H;\t1╬$L;\t1╣$R;\t1╠$h;\t1╫$l;\t1╢$r;\t1╟#box;\t1⧉#dL;\t1╕$R;\t1╒$l;\t1┐$r;\t1┌#h;\t1─$D;\t1╥$U;\t1╨$d;\t1┬$u;\t1┴#minus;\t1⊟#plus;\t1⊞#times;\t1⊠#uL;\t1╛$R;\t1╘$l;\t1┘$r;\t1└#v;\t1│$H;\t1╪$L;\t1╡$R;\t1╞$h;\t1┼$l;\t1┤$r;\t1├!prime;\t1‵!reve;\t1˘\"vbar\t1¦&;\t1¦!scr;\t2𝒷\"emi;\t1⁏\"im;\t1∽$e;\t1⋍\"ol;\t1\\$b;\t1⧅$hsub;\t1⟈!ull;\t1•$et;\t1•\"mp;\t1≎$E;\t1⪮$e;\t1≏%q;\t1≏ cacute;\t1ć\"p;\t1∩#and;\t1⩄#brcup;\t1⩉#cap;\t1⩋$up;\t1⩇#dot;\t1⩀#s;\t2∩︀\"ret;\t1⁁#on;\t1ˇ!caps;\t1⩍#ron;\t1č\"edil\t1ç&;\t1ç\"irc;\t1ĉ\"ups;\t1⩌%sm;\t1⩐!dot;\t1ċ!edil\t1¸%;\t1¸\"mptyv;\t1⦲\"nt\t1¢$;\t1¢$erdot;\t1·!fr;\t2𝔠!hcy;\t1ч\"eck;\t1✓%mark;\t1✓\"i;\t1χ!ir;\t1○#E;\t1⧃#c;\t1ˆ$eq;\t1≗$learrowleft;\t1↺+right;\t1↻&dR;\t1®'S;\t1Ⓢ'ast;\t1⊛'circ;\t1⊚'dash;\t1⊝#e;\t1≗#fnint;\t1⨐#mid;\t1⫯#scir;\t1⧂!lubs;\t1♣%uit;\t1♣!olon;\t1:%e;\t1≔&q;\t1≔\"mma;\t1,%t;\t1@#p;\t1∁$fn;\t1∘$lement;\t1∁&xes;\t1ℂ\"ng;\t1≅$dot;\t1⩭#int;\t1∮\"pf;\t2𝕔#rod;\t1∐#y\t1©$;\t1©$sr;\t1℗!rarr;\t1↵\"oss;\t1✗!scr;\t2𝒸\"ub;\t1⫏$e;\t1⫑#p;\t1⫐$e;\t1⫒!tdot;\t1⋯!udarrl;\t1⤸&r;\t1⤵\"epr;\t1⋞#sc;\t1⋟\"larr;\t1↶&p;\t1⤽\"p;\t1∪#brcap;\t1⩈#cap;\t1⩆$up;\t1⩊#dot;\t1⊍#or;\t1⩅#s;\t2∪︀\"rarr;\t1↷&m;\t1⤼#lyeqprec;\t1⋞'succ;\t1⋟%vee;\t1⋎%wedge;\t1⋏#ren\t1¤&;\t1¤#vearrowleft;\t1↶*right;\t1↷\"vee;\t1⋎\"wed;\t1⋏!wconint;\t1∲\"int;\t1∱!ylcty;\t1⌭ dArr;\t1⇓!Har;\t1⥥!agger;\t1†\"leth;\t1ℸ\"rr;\t1↓\"sh;\t1‐$v;\t1⊣!bkarow;\t1⤏\"lac;\t1˝!caron;\t1ď\"y;\t1д!d;\t1ⅆ\"agger;\t1‡#rr;\t1⇊\"otseq;\t1⩷!eg\t1°#;\t1°\"lta;\t1δ\"mptyv;\t1⦱!fisht;\t1⥿\"r;\t2𝔡!harl;\t1⇃$r;\t1⇂!iam;\t1⋄$ond;\t1⋄'suit;\t1♦$s;\t1♦\"e;\t1¨\"gamma;\t1ϝ\"sin;\t1⋲\"v;\t1÷#ide\t1÷&;\t1÷&ontimes;\t1⋇#onx;\t1⋇!jcy;\t1ђ!lcorn;\t1⌞#rop;\t1⌍!ollar;\t1$\"pf;\t2𝕕\"t;\t1˙#eq;\t1≐%dot;\t1≑#minus;\t1∸#plus;\t1∔#square;\t1⊡\"ublebarwedge;\t1⌆\"wnarrow;\t1↓$downarrows;\t1⇊$harpoonleft;\t1⇃+right;\t1⇂!rbkarow;\t1⤐\"corn;\t1⌟#rop;\t1⌌!scr;\t2𝒹#y;\t1ѕ\"ol;\t1⧶\"trok;\t1đ!tdot;\t1⋱\"ri;\t1▿$f;\t1▾!uarr;\t1⇵\"har;\t1⥯!wangle;\t1⦦!zcy;\t1џ\"igrarr;\t1⟿ eDDot;\t1⩷\"ot;\t1≑!acute\t1é&;\t1é\"ster;\t1⩮!caron;\t1ě\"ir;\t1≖$c\t1ê%;\t1ê\"olon;\t1≕\"y;\t1э!dot;\t1ė!e;\t1ⅇ!fDot;\t1≒\"r;\t2𝔢!g;\t1⪚\"rave\t1è&;\t1è\"s;\t1⪖#dot;\t1⪘!l;\t1⪙\"inters;\t1⏧\"l;\t1ℓ\"s;\t1⪕#dot;\t1⪗!macr;\t1ē\"pty;\t1∅%set;\t1∅%v;\t1∅\"sp13;\t1 %4;\t1 $;\t1 !ng;\t1ŋ\"sp;\t1 !ogon;\t1ę\"pf;\t2𝕖!par;\t1⋕$sl;\t1⧣\"lus;\t1⩱\"si;\t1ε$lon;\t1ε$v;\t1ϵ!qcirc;\t1≖#olon;\t1≕\"sim;\t1≂#lantgtr;\t1⪖'less;\t1⪕\"uals;\t1=#est;\t1≟#iv;\t1≡%DD;\t1⩸\"vparsl;\t1⧥!rDot;\t1≓\"arr;\t1⥱!scr;\t1ℯ\"dot;\t1≐\"im;\t1≂!ta;\t1η\"h\t1ð#;\t1ð!uml\t1ë$;\t1ë\"ro;\t1€!xcl;\t1!\"ist;\t1∃\"pectation;\t1ℰ#onentiale;\t1ⅇ fallingdotseq;\t1≒!cy;\t1ф!emale;\t1♀!filig;\t1ﬃ\"lig;\t1ﬀ#lig;\t1ﬄ\"r;\t2𝔣!ilig;\t1ﬁ!jlig;\t2fj!lat;\t1♭\"lig;\t1ﬂ\"tns;\t1▱!nof;\t1ƒ!opf;\t2𝕗\"rall;\t1∀#k;\t1⋔$v;\t1⫙!partint;\t1⨍!rac12\t1½&;\t1½%3;\t1⅓%4\t1¼&;\t1¼%5;\t1⅕%6;\t1⅙%8;\t1⅛$23;\t1⅔%5;\t1⅖$34\t1¾&;\t1¾%5;\t1⅗%8;\t1⅜$45;\t1⅘$56;\t1⅚%8;\t1⅝$78;\t1⅞#sl;\t1⁄\"own;\t1⌢!scr;\t2𝒻 gE;\t1≧\"l;\t1⪌!acute;\t1ǵ\"mma;\t1γ%d;\t1ϝ\"p;\t1⪆!breve;\t1ğ!circ;\t1ĝ\"y;\t1г!dot;\t1ġ!e;\t1≥\"l;\t1⋛\"q;\t1≥#q;\t1≧#slant;\t1⩾\"s;\t1⩾#cc;\t1⪩#dot;\t1⪀&o;\t1⪂'l;\t1⪄#l;\t2⋛︀$es;\t1⪔!fr;\t2𝔤!g;\t1≫\"g;\t1⋙!imel;\t1ℷ!jcy;\t1ѓ!l;\t1≷\"E;\t1⪒\"a;\t1⪥\"j;\t1⪤!nE;\t1≩\"ap;\t1⪊$prox;\t1⪊\"e;\t1⪈#q;\t1⪈$q;\t1≩\"sim;\t1⋧!opf;\t2𝕘!rave;\t1`!scr;\t1ℊ\"im;\t1≳$e;\t1⪎$l;\t1⪐!t\t1>\";\t1>\"cc;\t1⪧#ir;\t1⩺\"dot;\t1⋗\"lPar;\t1⦕\"quest;\t1⩼\"rapprox;\t1⪆$rr;\t1⥸#dot;\t1⋗#eqless;\t1⋛%qless;\t1⪌#less;\t1≷#sim;\t1≳!vertneqq;\t2≩︀\"nE;\t2≩︀ hArr;\t1⇔!airsp;\t1 \"lf;\t1½\"milt;\t1ℋ\"rdcy;\t1ъ#r;\t1↔$cir;\t1⥈$w;\t1↭!bar;\t1ℏ!circ;\t1ĥ!earts;\t1♥&uit;\t1♥\"llip;\t1…\"rcon;\t1⊹!fr;\t2𝔥!ksearow;\t1⤥#warow;\t1⤦!oarr;\t1⇿\"mtht;\t1∻\"okleftarrow;\t1↩$rightarrow;\t1↪\"pf;\t2𝕙\"rbar;\t1―!scr;\t2𝒽\"lash;\t1ℏ\"trok;\t1ħ!ybull;\t1⁃\"phen;\t1‐ iacute\t1í&;\t1í!c;\t1⁣\"irc\t1î%;\t1î\"y;\t1и!ecy;\t1е\"xcl\t1¡%;\t1¡!ff;\t1⇔\"r;\t2𝔦!grave\t1ì&;\t1ì!i;\t1ⅈ\"iint;\t1⨌#nt;\t1∭\"nfin;\t1⧜\"ota;\t1℩!jlig;\t1ĳ!macr;\t1ī#ge;\t1ℑ$line;\t1ℐ$part;\t1ℑ#th;\t1ı\"of;\t1⊷\"ped;\t1Ƶ!n;\t1∈\"care;\t1℅\"fin;\t1∞%tie;\t1⧝\"odot;\t1ı\"t;\t1∫#cal;\t1⊺#egers;\t1ℤ$rcal;\t1⊺#larhk;\t1⨗#prod;\t1⨼!ocy;\t1ё\"gon;\t1į\"pf;\t2𝕚\"ta;\t1ι!prod;\t1⨼!quest\t1¿&;\t1¿!scr;\t2𝒾\"in;\t1∈$E;\t1⋹$dot;\t1⋵$s;\t1⋴%v;\t1⋳$v;\t1∈!t;\t1⁢\"ilde;\t1ĩ!ukcy;\t1і\"ml\t1ï$;\t1ï jcirc;\t1ĵ\"y;\t1й!fr;\t2𝔧!math;\t1ȷ!opf;\t2𝕛!scr;\t2𝒿\"ercy;\t1ј!ukcy;\t1є kappa;\t1κ%v;\t1ϰ!cedil;\t1ķ\"y;\t1к!fr;\t2𝔨!green;\t1ĸ!hcy;\t1х!jcy;\t1ќ!opf;\t2𝕜!scr;\t2𝓀 lAarr;\t1⇚\"rr;\t1⇐\"tail;\t1⤛!Barr;\t1⤎!E;\t1≦\"g;\t1⪋!Har;\t1⥢!acute;\t1ĺ\"emptyv;\t1⦴\"gran;\t1ℒ\"mbda;\t1λ\"ng;\t1⟨$d;\t1⦑$le;\t1⟨\"p;\t1⪅\"quo\t1«%;\t1«\"rr;\t1←$b;\t1⇤%fs;\t1⤟$fs;\t1⤝$hk;\t1↩$lp;\t1↫$pl;\t1⤹$sim;\t1⥳$tl;\t1↢\"t;\t1⪫#ail;\t1⤙#e;\t1⪭$s;\t2⪭︀!barr;\t1⤌\"brk;\t1❲\"race;\t1{%k;\t1[#ke;\t1⦋$sld;\t1⦏&u;\t1⦍!caron;\t1ľ\"edil;\t1ļ#il;\t1⌈\"ub;\t1{\"y;\t1л!dca;\t1⤶\"quo;\t1“%r;\t1„\"rdhar;\t1⥧#ushar;\t1⥋\"sh;\t1↲!e;\t1≤\"ftarrow;\t1←)tail;\t1↢$harpoondown;\t1↽+up;\t1↼$leftarrows;\t1⇇$rightarrow;\t1↔.s;\t1⇆)harpoons;\t1⇋)squigarrow;\t1↭$threetimes;\t1⋋\"g;\t1⋚\"q;\t1≤#q;\t1≦#slant;\t1⩽\"s;\t1⩽#cc;\t1⪨#dot;\t1⩿&o;\t1⪁'r;\t1⪃#g;\t2⋚︀$es;\t1⪓#sapprox;\t1⪅$dot;\t1⋖$eqgtr;\t1⋚&qgtr;\t1⪋$gtr;\t1≶$sim;\t1≲!fisht;\t1⥼\"loor;\t1⌊\"r;\t2𝔩!g;\t1≶\"E;\t1⪑!hard;\t1↽$u;\t1↼%l;\t1⥪\"blk;\t1▄!jcy;\t1љ!l;\t1≪\"arr;\t1⇇\"corner;\t1⌞\"hard;\t1⥫\"tri;\t1◺!midot;\t1ŀ\"oust;\t1⎰&ache;\t1⎰!nE;\t1≨\"ap;\t1⪉$prox;\t1⪉\"e;\t1⪇#q;\t1⪇$q;\t1≨\"sim;\t1⋦!oang;\t1⟬#rr;\t1⇽\"brk;\t1⟦\"ngleftarrow;\t1⟵(rightarrow;\t1⟷$mapsto;\t1⟼$rightarrow;\t1⟶\"oparrowleft;\t1↫)right;\t1↬\"par;\t1⦅#f;\t2𝕝#lus;\t1⨭\"times;\t1⨴\"wast;\t1∗#bar;\t1_\"z;\t1◊#enge;\t1◊#f;\t1⧫!par;\t1($lt;\t1⦓!rarr;\t1⇆\"corner;\t1⌟\"har;\t1⇋%d;\t1⥭\"m;\t1‎\"tri;\t1⊿!saquo;\t1‹\"cr;\t2𝓁\"h;\t1↰\"im;\t1≲$e;\t1⪍$g;\t1⪏\"qb;\t1[#uo;\t1‘%r;\t1‚\"trok;\t1ł!t\t1<\";\t1<\"cc;\t1⪦#ir;\t1⩹\"dot;\t1⋖\"hree;\t1⋋\"imes;\t1⋉\"larr;\t1⥶\"quest;\t1⩻\"rPar;\t1⦖#i;\t1◃$e;\t1⊴$f;\t1◂!urdshar;\t1⥊#uhar;\t1⥦!vertneqq;\t2≨︀\"nE;\t2≨︀ mDDot;\t1∺!acr\t1¯$;\t1¯\"le;\t1♂#t;\t1✠$ese;\t1✠\"p;\t1↦#sto;\t1↦&down;\t1↧&left;\t1↤&up;\t1↥\"rker;\t1▮!comma;\t1⨩\"y;\t1м!dash;\t1—!easuredangle;\t1∡!fr;\t2𝔪!ho;\t1℧!icro\t1µ%;\t1µ\"d;\t1∣#ast;\t1*#cir;\t1⫰#dot\t1·&;\t1·\"nus;\t1−%b;\t1⊟%d;\t1∸&u;\t1⨪!lcp;\t1⫛\"dr;\t1…!nplus;\t1∓!odels;\t1⊧\"pf;\t2𝕞!p;\t1∓!scr;\t2𝓂\"tpos;\t1∾!u;\t1μ\"ltimap;\t1⊸\"map;\t1⊸ nGg;\t2⋙̸\"t;\t2≫⃒#v;\t2≫̸!Leftarrow;\t1⇍%rightarrow;\t1⇎\"l;\t2⋘̸\"t;\t2≪⃒#v;\t2≪̸!Rightarrow;\t1⇏!VDash;\t1⊯\"dash;\t1⊮!abla;\t1∇\"cute;\t1ń\"ng;\t2∠⃒\"p;\t1≉#E;\t2⩰̸#id;\t2≋̸#os;\t1ŉ#prox;\t1≉\"tur;\t1♮%al;\t1♮'s;\t1ℕ!bsp\t1 $;\t1 \"ump;\t2≎̸%e;\t2≏̸!cap;\t1⩃#ron;\t1ň\"edil;\t1ņ\"ong;\t1≇%dot;\t2⩭̸\"up;\t1⩂\"y;\t1н!dash;\t1–!e;\t1≠\"Arr;\t1⇗\"arhk;\t1⤤$r;\t1↗%ow;\t1↗\"dot;\t2≐̸\"quiv;\t1≢\"sear;\t1⤨#im;\t2≂̸\"xist;\t1∄&s;\t1∄!fr;\t2𝔫!gE;\t2≧̸\"e;\t1≱#q;\t1≱$q;\t2≧̸$slant;\t2⩾̸#s;\t2⩾̸\"sim;\t1≵\"t;\t1≯#r;\t1≯!hArr;\t1⇎\"arr;\t1↮\"par;\t1⫲!i;\t1∋\"s;\t1⋼#d;\t1⋺\"v;\t1∋!jcy;\t1њ!lArr;\t1⇍\"E;\t2≦̸\"arr;\t1↚\"dr;\t1‥\"e;\t1≰#ftarrow;\t1↚%rightarrow;\t1↮#q;\t1≰$q;\t2≦̸$slant;\t2⩽̸#s;\t2⩽̸$s;\t1≮\"sim;\t1≴\"t;\t1≮#ri;\t1⋪%e;\t1⋬!mid;\t1∤!opf;\t2𝕟\"t\t1¬#;\t1¬#in;\t1∉%E;\t2⋹̸%dot;\t2⋵̸%va;\t1∉&b;\t1⋷&c;\t1⋶#ni;\t1∌%va;\t1∌&b;\t1⋾&c;\t1⋽!par;\t1∦$allel;\t1∦$sl;\t2⫽⃥$t;\t2∂̸\"olint;\t1⨔\"r;\t1⊀#cue;\t1⋠#e;\t2⪯̸$c;\t1⊀%eq;\t2⪯̸!rArr;\t1⇏\"arr;\t1↛%c;\t2⤳̸%w;\t2↝̸\"ightarrow;\t1↛\"tri;\t1⋫%e;\t1⋭!sc;\t1⊁#cue;\t1⋡#e;\t2⪰̸#r;\t2𝓃\"hortmid;\t1∤&parallel;\t1∦\"im;\t1≁$e;\t1≄%q;\t1≄\"mid;\t1∤\"par;\t1∦\"qsube;\t1⋢%pe;\t1⋣\"ub;\t1⊄$E;\t2⫅̸$e;\t1⊈$set;\t2⊂⃒'eq;\t1⊈)q;\t2⫅̸#cc;\t1⊁%eq;\t2⪰̸#p;\t1⊅$E;\t2⫆̸$e;\t1⊉$set;\t2⊃⃒'eq;\t1⊉)q;\t2⫆̸!tgl;\t1≹\"ilde\t1ñ&;\t1ñ\"lg;\t1≸\"riangleleft;\t1⋪-eq;\t1⋬)right;\t1⋫.eq;\t1⋭!u;\t1ν\"m;\t1##ero;\t1№#sp;\t1 !vDash;\t1⊭\"Harr;\t1⤄\"ap;\t2≍⃒\"dash;\t1⊬\"ge;\t2≥⃒#t;\t2>⃒\"infin;\t1⧞\"lArr;\t1⤂#e;\t2≤⃒#t;\t2<⃒$rie;\t2⊴⃒\"rArr;\t1⤃#trie;\t2⊵⃒\"sim;\t2∼⃒!wArr;\t1⇖\"arhk;\t1⤣$r;\t1↖%ow;\t1↖\"near;\t1⤧ oS;\t1Ⓢ!acute\t1ó&;\t1ó\"st;\t1⊛!cir;\t1⊚$c\t1ô%;\t1ô\"y;\t1о!dash;\t1⊝\"blac;\t1ő\"iv;\t1⨸\"ot;\t1⊙\"sold;\t1⦼!elig;\t1œ!fcir;\t1⦿\"r;\t2𝔬!gon;\t1˛\"rave\t1ò&;\t1ò\"t;\t1⧁!hbar;\t1⦵\"m;\t1Ω!int;\t1∮!larr;\t1↺\"cir;\t1⦾#ross;\t1⦻\"ine;\t1‾\"t;\t1⧀!macr;\t1ō\"ega;\t1ω\"icron;\t1ο#d;\t1⦶#nus;\t1⊖!opf;\t2𝕠!par;\t1⦷\"erp;\t1⦹\"lus;\t1⊕!r;\t1∨\"arr;\t1↻\"d;\t1⩝#er;\t1ℴ%of;\t1ℴ#f\t1ª$;\t1ª#m\t1º$;\t1º\"igof;\t1⊶\"or;\t1⩖\"slope;\t1⩗\"v;\t1⩛!scr;\t1ℴ\"lash\t1ø&;\t1ø\"ol;\t1⊘!tilde\t1õ&;\t1õ#mes;\t1⊗&as;\t1⨶!uml\t1ö$;\t1ö!vbar;\t1⌽ par;\t1∥#a\t1¶$;\t1¶$llel;\t1∥#sim;\t1⫳$l;\t1⫽#t;\t1∂!cy;\t1п!ercnt;\t1%#iod;\t1.#mil;\t1‰#p;\t1⊥#tenk;\t1‱!fr;\t2𝔭!hi;\t1φ#v;\t1ϕ\"mmat;\t1ℳ\"one;\t1☎!i;\t1π\"tchfork;\t1⋔\"v;\t1ϖ!lanck;\t1ℏ&h;\t1ℎ$kv;\t1ℏ\"us;\t1+$acir;\t1⨣$b;\t1⊞$cir;\t1⨢$do;\t1∔%u;\t1⨥$e;\t1⩲$mn\t1±&;\t1±$sim;\t1⨦$two;\t1⨧!m;\t1±!ointint;\t1⨕\"pf;\t2𝕡\"und\t1£%;\t1£!r;\t1≺\"E;\t1⪳\"ap;\t1⪷\"cue;\t1≼\"e;\t1⪯#c;\t1≺$approx;\t1⪷$curlyeq;\t1≼$eq;\t1⪯$napprox;\t1⪹%eqq;\t1⪵%sim;\t1⋨$sim;\t1≾\"ime;\t1′%s;\t1ℙ\"nE;\t1⪵#ap;\t1⪹#sim;\t1⋨\"od;\t1∏#falar;\t1⌮$line;\t1⌒$surf;\t1⌓#p;\t1∝$to;\t1∝\"sim;\t1≾\"urel;\t1⊰!scr;\t2𝓅\"i;\t1ψ!uncsp;\t1  qfr;\t2𝔮!int;\t1⨌!opf;\t2𝕢!prime;\t1⁗!scr;\t2𝓆!uaternions;\t1ℍ$int;\t1⨖\"est;\t1?%eq;\t1≟\"ot\t1\"$;\t1\" rAarr;\t1⇛\"rr;\t1⇒\"tail;\t1⤜!Barr;\t1⤏!Har;\t1⥤!ace;\t2∽̱#ute;\t1ŕ\"dic;\t1√\"emptyv;\t1⦳\"ng;\t1⟩$d;\t1⦒$e;\t1⦥$le;\t1⟩\"quo\t1»%;\t1»\"rr;\t1→$ap;\t1⥵$b;\t1⇥%fs;\t1⤠$c;\t1⤳$fs;\t1⤞$hk;\t1↪$lp;\t1↬$pl;\t1⥅$sim;\t1⥴$tl;\t1↣$w;\t1↝\"tail;\t1⤚#io;\t1∶%nals;\t1ℚ!barr;\t1⤍\"brk;\t1❳\"race;\t1}%k;\t1]#ke;\t1⦌$sld;\t1⦎&u;\t1⦐!caron;\t1ř\"edil;\t1ŗ#il;\t1⌉\"ub;\t1}\"y;\t1р!dca;\t1⤷\"ldhar;\t1⥩\"quo;\t1”%r;\t1”\"sh;\t1↳!eal;\t1ℜ$ine;\t1ℛ$part;\t1ℜ$s;\t1ℝ\"ct;\t1▭\"g\t1®#;\t1®!fisht;\t1⥽\"loor;\t1⌋\"r;\t2𝔯!hard;\t1⇁$u;\t1⇀%l;\t1⥬\"o;\t1ρ#v;\t1ϱ!ightarrow;\t1→*tail;\t1↣%harpoondown;\t1⇁,up;\t1⇀%leftarrows;\t1⇄)harpoons;\t1⇌%rightarrows;\t1⇉%squigarrow;\t1↝%threetimes;\t1⋌\"ng;\t1˚\"singdotseq;\t1≓!larr;\t1⇄\"har;\t1⇌\"m;\t1‏!moust;\t1⎱&ache;\t1⎱!nmid;\t1⫮!oang;\t1⟭#rr;\t1⇾\"brk;\t1⟧\"par;\t1⦆#f;\t2𝕣#lus;\t1⨮\"times;\t1⨵!par;\t1)$gt;\t1⦔\"polint;\t1⨒!rarr;\t1⇉!saquo;\t1›\"cr;\t2𝓇\"h;\t1↱\"qb;\t1]#uo;\t1’%r;\t1’!three;\t1⋌\"imes;\t1⋊\"ri;\t1▹$e;\t1⊵$f;\t1▸$ltri;\t1⧎!uluhar;\t1⥨!x;\t1℞ sacute;\t1ś!bquo;\t1‚!c;\t1≻\"E;\t1⪴\"ap;\t1⪸#ron;\t1š\"cue;\t1≽\"e;\t1⪰#dil;\t1ş\"irc;\t1ŝ\"nE;\t1⪶#ap;\t1⪺#sim;\t1⋩\"polint;\t1⨓\"sim;\t1≿\"y;\t1с!dot;\t1⋅$b;\t1⊡$e;\t1⩦!eArr;\t1⇘\"arhk;\t1⤥$r;\t1↘%ow;\t1↘\"ct\t1§$;\t1§\"mi;\t1;\"swar;\t1⤩\"tminus;\t1∖$n;\t1∖\"xt;\t1✶!fr;\t2𝔰#own;\t1⌢!harp;\t1♯\"chcy;\t1щ#y;\t1ш\"ortmid;\t1∣%parallel;\t1∥\"y\t1­#;\t1­!igma;\t1σ%f;\t1ς%v;\t1ς\"m;\t1∼#dot;\t1⩪#e;\t1≃$q;\t1≃#g;\t1⪞$E;\t1⪠#l;\t1⪝$E;\t1⪟#ne;\t1≆#plus;\t1⨤#rarr;\t1⥲!larr;\t1←!mallsetminus;\t1∖#shp;\t1⨳\"eparsl;\t1⧤\"id;\t1∣#le;\t1⌣\"t;\t1⪪#e;\t1⪬$s;\t2⪬︀!oftcy;\t1ь\"l;\t1/#b;\t1⧄$ar;\t1⌿\"pf;\t2𝕤!pades;\t1♠&uit;\t1♠#r;\t1∥!qcap;\t1⊓%s;\t2⊓︀#up;\t1⊔%s;\t2⊔︀\"sub;\t1⊏%e;\t1⊑%set;\t1⊏(eq;\t1⊑$p;\t1⊐%e;\t1⊒%set;\t1⊐(eq;\t1⊒\"u;\t1□#are;\t1□%f;\t1▪#f;\t1▪!rarr;\t1→!scr;\t2𝓈\"etmn;\t1∖\"mile;\t1⌣\"tarf;\t1⋆!tar;\t1☆$f;\t1★\"raightepsilon;\t1ϵ(phi;\t1ϕ#ns;\t1¯!ub;\t1⊂#E;\t1⫅#dot;\t1⪽#e;\t1⊆$dot;\t1⫃#mult;\t1⫁#nE;\t1⫋$e;\t1⊊#plus;\t1⪿#rarr;\t1⥹#set;\t1⊂&eq;\t1⊆(q;\t1⫅&neq;\t1⊊)q;\t1⫋$im;\t1⫇$ub;\t1⫕%p;\t1⫓\"cc;\t1≻$approx;\t1⪸$curlyeq;\t1≽$eq;\t1⪰$napprox;\t1⪺%eqq;\t1⪶%sim;\t1⋩$sim;\t1≿\"m;\t1∑\"ng;\t1♪\"p1\t1¹$;\t1¹#2\t1²$;\t1²#3\t1³$;\t1³#;\t1⊃#E;\t1⫆#dot;\t1⪾$sub;\t1⫘#e;\t1⊇$dot;\t1⫄#hsol;\t1⟉%ub;\t1⫗#larr;\t1⥻#mult;\t1⫂#nE;\t1⫌$e;\t1⊋#plus;\t1⫀#set;\t1⊃&eq;\t1⊇(q;\t1⫆&neq;\t1⊋)q;\t1⫌$im;\t1⫈$ub;\t1⫔%p;\t1⫖!wArr;\t1⇙\"arhk;\t1⤦$r;\t1↙%ow;\t1↙\"nwar;\t1⤪!zlig\t1ß%;\t1ß target;\t1⌖\"u;\t1τ!brk;\t1⎴!caron;\t1ť\"edil;\t1ţ\"y;\t1т!dot;\t1⃛!elrec;\t1⌕!fr;\t2𝔱!here4;\t1∴%fore;\t1∴#ta;\t1θ%sym;\t1ϑ%v;\t1ϑ\"ickapprox;\t1≈%sim;\t1∼#nsp;\t1 \"kap;\t1≈#sim;\t1∼\"orn\t1þ%;\t1þ!ilde;\t1˜\"mes\t1×%;\t1×%b;\t1⊠&ar;\t1⨱%d;\t1⨰\"nt;\t1∭!oea;\t1⤨\"p;\t1⊤#bot;\t1⌶#cir;\t1⫱#f;\t2𝕥$ork;\t1⫚\"sa;\t1⤩!prime;\t1‴!rade;\t1™\"iangle;\t1▵(down;\t1▿(left;\t1◃,eq;\t1⊴(q;\t1≜(right;\t1▹-eq;\t1⊵#dot;\t1◬#e;\t1≜#minus;\t1⨺#plus;\t1⨹#sb;\t1⧍#time;\t1⨻\"pezium;\t1⏢!scr;\t2𝓉#y;\t1ц\"hcy;\t1ћ\"trok;\t1ŧ!wixt;\t1≬\"oheadleftarrow;\t1↞'rightarrow;\t1↠ uArr;\t1⇑!Har;\t1⥣!acute\t1ú&;\t1ú\"rr;\t1↑!brcy;\t1ў#eve;\t1ŭ!circ\t1û%;\t1û\"y;\t1у!darr;\t1⇅\"blac;\t1ű\"har;\t1⥮!fisht;\t1⥾\"r;\t2𝔲!grave\t1ù&;\t1ù!harl;\t1↿$r;\t1↾\"blk;\t1▀!lcorn;\t1⌜&er;\t1⌜#rop;\t1⌏\"tri;\t1◸!macr;\t1ū\"l\t1¨#;\t1¨!ogon;\t1ų\"pf;\t2𝕦!parrow;\t1↑\"downarrow;\t1↕\"harpoonleft;\t1↿)right;\t1↾\"lus;\t1⊎\"si;\t1υ$h;\t1ϒ$lon;\t1υ\"uparrows;\t1⇈!rcorn;\t1⌝&er;\t1⌝#rop;\t1⌎\"ing;\t1ů\"tri;\t1◹!scr;\t2𝓊!tdot;\t1⋰\"ilde;\t1ũ\"ri;\t1▵$f;\t1▴!uarr;\t1⇈\"ml\t1ü$;\t1ü!wangle;\t1⦧ vArr;\t1⇕!Bar;\t1⫨$v;\t1⫩!Dash;\t1⊨!angrt;\t1⦜\"repsilon;\t1ϵ#kappa;\t1ϰ#nothing;\t1∅#phi;\t1ϕ$i;\t1ϖ$ropto;\t1∝#r;\t1↕$ho;\t1ϱ#sigma;\t1ς$ubsetneq;\t2⊊︀,q;\t2⫋︀%psetneq;\t2⊋︀,q;\t2⫌︀#theta;\t1ϑ$riangleleft;\t1⊲+right;\t1⊳!cy;\t1в!dash;\t1⊢!ee;\t1∨#bar;\t1⊻#eq;\t1≚\"llip;\t1⋮\"rbar;\t1|#t;\t1|!fr;\t2𝔳!ltri;\t1⊲!nsub;\t2⊂⃒$p;\t2⊃⃒!opf;\t2𝕧!prop;\t1∝!rtri;\t1⊳!scr;\t2𝓋\"ubnE;\t2⫋︀%e;\t2⊊︀#pnE;\t2⫌︀%e;\t2⊋︀!zigzag;\t1⦚ wcirc;\t1ŵ!edbar;\t1⩟#ge;\t1∧%q;\t1≙\"ierp;\t1℘!fr;\t2𝔴!opf;\t2𝕨!p;\t1℘!r;\t1≀\"eath;\t1≀!scr;\t2𝓌 xcap;\t1⋂\"irc;\t1◯\"up;\t1⋃!dtri;\t1▽!fr;\t2𝔵!hArr;\t1⟺\"arr;\t1⟷!i;\t1ξ!lArr;\t1⟸\"arr;\t1⟵!map;\t1⟼!nis;\t1⋻!odot;\t1⨀\"pf;\t2𝕩#lus;\t1⨁\"time;\t1⨂!rArr;\t1⟹\"arr;\t1⟶!scr;\t2𝓍\"qcup;\t1⨆!uplus;\t1⨄\"tri;\t1△!vee;\t1⋁!wedge;\t1⋀ yacute\t1ý&;\t1ý#y;\t1я!circ;\t1ŷ\"y;\t1ы!en\t1¥#;\t1¥!fr;\t2𝔶!icy;\t1ї!opf;\t2𝕪!scr;\t2𝓎!ucy;\t1ю\"ml\t1ÿ$;\t1ÿ zacute;\t1ź!caron;\t1ž\"y;\t1з!dot;\t1ż!eetrf;\t1ℨ\"ta;\t1ζ!fr;\t2𝔷!hcy;\t1ж!igrarr;\t1⇝!opf;\t2𝕫!scr;\t2𝓏!wj;\t1‍\"nj;\t1‌";

/** @type {Readonly<Record<string, string>> | undefined} */
let htmlEntityCache;

/**
 * Named character references defined by the WHATWG HTML Standard. Decoded
 * lazily from `HTML_ENTITY_SOURCE` on first call and cached.
 * @returns {Readonly<Record<string, string>>} entity name to characters
 */
const getHtmlEntities = () => {
	if (htmlEntityCache !== undefined) return htmlEntityCache;
	/** @type {Record<string, string>} */
	const map = Object.create(null);
	let prev = "";
	let i = 0;
	while (i < HTML_ENTITY_SOURCE.length) {
		const shared = HTML_ENTITY_SOURCE.charCodeAt(i++) - 0x20;
		const t = HTML_ENTITY_SOURCE.indexOf("	", i);
		const name = prev.slice(0, shared) + HTML_ENTITY_SOURCE.slice(i, t);
		const valueLen = HTML_ENTITY_SOURCE.charCodeAt(t + 1) - 0x30;
		map[name] = HTML_ENTITY_SOURCE.slice(t + 2, t + 2 + valueLen);
		prev = name;
		i = t + 2 + valueLen;
	}
	htmlEntityCache = Object.freeze(map);
	return htmlEntityCache;
};
// #endregion

const STATE_DATA = 0;
const STATE_TAG_OPEN = 1;
const STATE_END_TAG_OPEN = 2;
const STATE_TAG_NAME = 3;
const STATE_BEFORE_ATTRIBUTE_NAME = 4;
const STATE_ATTRIBUTE_NAME = 5;
const STATE_AFTER_ATTRIBUTE_NAME = 6;
const STATE_BEFORE_ATTRIBUTE_VALUE = 7;
const STATE_ATTRIBUTE_VALUE_DOUBLE_QUOTED = 8;
const STATE_ATTRIBUTE_VALUE_SINGLE_QUOTED = 9;
const STATE_ATTRIBUTE_VALUE_UNQUOTED = 10;
const STATE_AFTER_ATTRIBUTE_VALUE_QUOTED = 11;
const STATE_SELF_CLOSING_START_TAG = 12;

const STATE_MARKUP_DECLARATION_OPEN = 13;
const STATE_COMMENT_START = 14;
const STATE_COMMENT_START_DASH = 15;
const STATE_COMMENT = 16;
const STATE_COMMENT_END_DASH = 17;
const STATE_COMMENT_END = 18;
const STATE_COMMENT_END_BANG = 19;
const STATE_BOGUS_COMMENT = 20;

const STATE_COMMENT_LESS_THAN_SIGN = 21;
const STATE_COMMENT_LESS_THAN_SIGN_BANG = 22;
const STATE_COMMENT_LESS_THAN_SIGN_BANG_DASH = 23;
const STATE_COMMENT_LESS_THAN_SIGN_BANG_DASH_DASH = 24;

const STATE_DOCTYPE = 25;
const STATE_BEFORE_DOCTYPE_NAME = 26;
const STATE_DOCTYPE_NAME = 27;
const STATE_AFTER_DOCTYPE_NAME = 28;
const STATE_AFTER_DOCTYPE_PUBLIC_KEYWORD = 29;
const STATE_BEFORE_DOCTYPE_PUBLIC_IDENTIFIER = 30;
const STATE_DOCTYPE_PUBLIC_IDENTIFIER_DOUBLE_QUOTED = 31;
const STATE_DOCTYPE_PUBLIC_IDENTIFIER_SINGLE_QUOTED = 32;
const STATE_AFTER_DOCTYPE_PUBLIC_IDENTIFIER = 33;
const STATE_BETWEEN_DOCTYPE_PUBLIC_AND_SYSTEM_IDENTIFIERS = 34;
const STATE_AFTER_DOCTYPE_SYSTEM_KEYWORD = 35;
const STATE_BEFORE_DOCTYPE_SYSTEM_IDENTIFIER = 36;
const STATE_DOCTYPE_SYSTEM_IDENTIFIER_DOUBLE_QUOTED = 37;
const STATE_DOCTYPE_SYSTEM_IDENTIFIER_SINGLE_QUOTED = 38;
const STATE_AFTER_DOCTYPE_SYSTEM_IDENTIFIER = 39;
const STATE_BOGUS_DOCTYPE = 40;

const STATE_CDATA_SECTION = 41;
const STATE_CDATA_SECTION_BRACKET = 42;
const STATE_CDATA_SECTION_END = 43;

const STATE_RCDATA = 44;
const STATE_RCDATA_LESS_THAN_SIGN = 45;
const STATE_RCDATA_END_TAG_OPEN = 46;
const STATE_RCDATA_END_TAG_NAME = 47;

const STATE_RAWTEXT = 48;
const STATE_RAWTEXT_LESS_THAN_SIGN = 49;
const STATE_RAWTEXT_END_TAG_OPEN = 50;
const STATE_RAWTEXT_END_TAG_NAME = 51;

const STATE_SCRIPT_DATA = 52;
const STATE_SCRIPT_DATA_LESS_THAN_SIGN = 53;
const STATE_SCRIPT_DATA_END_TAG_OPEN = 54;
const STATE_SCRIPT_DATA_END_TAG_NAME = 55;
const STATE_SCRIPT_DATA_ESCAPE_START = 56;
const STATE_SCRIPT_DATA_ESCAPE_START_DASH = 57;
const STATE_SCRIPT_DATA_ESCAPED = 58;
const STATE_SCRIPT_DATA_ESCAPED_DASH = 59;
const STATE_SCRIPT_DATA_ESCAPED_DASH_DASH = 60;
const STATE_SCRIPT_DATA_ESCAPED_LESS_THAN_SIGN = 61;
const STATE_SCRIPT_DATA_ESCAPED_END_TAG_OPEN = 62;
const STATE_SCRIPT_DATA_ESCAPED_END_TAG_NAME = 63;
const STATE_SCRIPT_DATA_DOUBLE_ESCAPE_START = 64;
const STATE_SCRIPT_DATA_DOUBLE_ESCAPED = 65;
const STATE_SCRIPT_DATA_DOUBLE_ESCAPED_DASH = 66;
const STATE_SCRIPT_DATA_DOUBLE_ESCAPED_DASH_DASH = 67;
const STATE_SCRIPT_DATA_DOUBLE_ESCAPED_LESS_THAN_SIGN = 68;
const STATE_SCRIPT_DATA_DOUBLE_ESCAPE_END = 69;

const STATE_PLAINTEXT = 70;

// https://html.spec.whatwg.org/multipage/parsing.html#character-reference-state
const STATE_CHARACTER_REFERENCE = 71;
// https://html.spec.whatwg.org/multipage/parsing.html#named-character-reference-state
const STATE_NAMED_CHARACTER_REFERENCE = 72;
// https://html.spec.whatwg.org/multipage/parsing.html#ambiguous-ampersand-state
const STATE_AMBIGUOUS_AMPERSAND = 73;
// https://html.spec.whatwg.org/multipage/parsing.html#numeric-character-reference-state
const STATE_NUMERIC_CHARACTER_REFERENCE = 74;
// https://html.spec.whatwg.org/multipage/parsing.html#hexadecimal-character-reference-start-state
const STATE_HEXADECIMAL_CHARACTER_REFERENCE_START = 75;
// https://html.spec.whatwg.org/multipage/parsing.html#decimal-character-reference-start-state
const STATE_DECIMAL_CHARACTER_REFERENCE_START = 76;
// https://html.spec.whatwg.org/multipage/parsing.html#hexadecimal-character-reference-state
const STATE_HEXADECIMAL_CHARACTER_REFERENCE = 77;
// https://html.spec.whatwg.org/multipage/parsing.html#decimal-character-reference-state
const STATE_DECIMAL_CHARACTER_REFERENCE = 78;
// https://html.spec.whatwg.org/multipage/parsing.html#numeric-character-reference-end-state
const STATE_NUMERIC_CHARACTER_REFERENCE_END = 79;

const CC_TAB = 0x09;
const CC_LF = 0x0a;
const CC_FF = 0x0c;
const CC_SPACE = 0x20;
const CC_EXCLAMATION_MARK = 0x21;
const CC_QUOTATION_MARK = 0x22;
const CC_NUMBER_SIGN = 0x23;
const CC_AMPERSAND = 0x26;
const CC_APOSTROPHE = 0x27;
const CC_HYPHEN_MINUS = 0x2d;
const CC_SOLIDUS = 0x2f;
const CC_SEMICOLON = 0x3b;
const CC_LESS_THAN = 0x3c;
const CC_EQUALS = 0x3d;
const CC_GREATER_THAN = 0x3e;
const CC_QUESTION_MARK = 0x3f;
const CC_LEFT_SQUARE_BRACKET = 0x5b;
const CC_RIGHT_SQUARE_BRACKET = 0x5d;

const QUOTE_DOUBLE = 1;
const QUOTE_SINGLE = 2;
const QUOTE_NONE = 0;

/**
 * @param {number} cc character code
 * @returns {boolean} is ascii alpha
 */
const isAsciiAlpha = (cc) =>
	(cc >= 0x41 && cc <= 0x5a) || (cc >= 0x61 && cc <= 0x7a);

/**
 * @param {number} cc character code
 * @returns {boolean} is ascii alphanumeric
 */
const isAsciiAlphanumeric = (cc) =>
	isAsciiAlpha(cc) || (cc >= 0x30 && cc <= 0x39);

/**
 * @param {number} cc character code
 * @returns {boolean} is ascii digit
 */
const isAsciiDigit = (cc) => cc >= 0x30 && cc <= 0x39;

/**
 * @param {number} cc character code
 * @returns {boolean} is ascii hex digit
 */
const isAsciiHexDigit = (cc) =>
	(cc >= 0x30 && cc <= 0x39) ||
	(cc >= 0x41 && cc <= 0x46) ||
	(cc >= 0x61 && cc <= 0x66);

/**
 * @param {number} cc character code
 * @returns {boolean} is space
 */
const isSpace = (cc) =>
	cc === CC_TAB || cc === CC_LF || cc === CC_FF || cc === CC_SPACE;

/**
 * Severity of a tokenizer-detected parse error. `"warning"` is recoverable
 * (the tokenizer continued and the emitted token is still well-formed, e.g.
 * missing-attribute-value); `"error"` means the emitted token byte range is
 * incomplete or does not match what the spec would produce, e.g. eof-in-tag.
 * @typedef {"warning" | "error"} ParseErrorSeverity
 */

/**
 * @typedef {object} HtmlTokenCallbacks
 * @property {(input: string, start: number, end: number, nameStart: number, nameEnd: number, selfClosing: boolean) => number=} openTag
 * @property {(input: string, start: number, end: number, nameStart: number, nameEnd: number) => number=} closeTag
 * @property {(input: string, start: number, end: number) => number=} text
 * @property {(input: string, nameStart: number, nameEnd: number, valueStart: number, valueEnd: number, quoteType: number) => number=} attribute
 * @property {(input: string, start: number, end: number) => number=} comment
 * @property {(input: string, start: number, end: number) => number=} doctype
 * @property {(input: string, code: string, start: number, end: number, severity: ParseErrorSeverity) => void=} parseError
 */

/**
 * @param {string} input input string
 * @param {number} pos current position
 * @param {HtmlTokenCallbacks} callbacks callbacks
 * @returns {number} final position
 */
const walkHtmlTokens = (input, pos = 0, callbacks = {}) => {
	const len = input.length;
	let state = STATE_DATA;
	let returnState = STATE_DATA;

	let textStart = pos;
	let tagStart = pos;
	let tagNameStart = -1;
	let tagNameEnd = -1;
	let attrNameStart = -1;
	let attrNameEnd = -1;
	let attrValueStart = -1;
	let attrQuoteType = QUOTE_NONE;
	let commentStart = pos;
	let lastOpenTagName = "";
	let tempBuffer = "";
	let namedEntityConsumed = 0;

	/**
	 * Reports a tokenizer parse error to the consumer. The byte range and
	 * severity follow the WHATWG spec naming. Severity is `"error"` for
	 * cases where the emitted token is incomplete (EOF inside a tag or
	 * comment); everything else is a `"warning"`.
	 * @param {string} code WHATWG parse-error code (kebab-case)
	 * @param {number} start byte offset where the error starts
	 * @param {number} end byte offset where the error ends
	 * @param {ParseErrorSeverity} severity error severity
	 */
	const reportError = (code, start, end, severity) => {
		if (callbacks.parseError !== undefined) {
			callbacks.parseError(input, code, start, end, severity);
		}
	};

	/**
	 * @param {number} cc character code
	 * @returns {boolean} is ascii lower alpha
	 */
	const isAsciiLowerAlpha = (cc) => cc >= 0x61 && cc <= 0x7a;

	/**
	 * @param {number} cc character code
	 * @returns {boolean} is ascii upper alpha
	 */
	const isAsciiUpperAlpha = (cc) => cc >= 0x41 && cc <= 0x5a;

	/**
	 * @param {string} name tag name (lowercase)
	 * @returns {number} content mode state for this tag, or STATE_DATA
	 */
	const getContentModeForTag = (name) => {
		switch (name) {
			case "textarea":
			case "title":
				return STATE_RCDATA;
			case "style":
			case "xmp":
			case "iframe":
			case "noembed":
			case "noframes":
				return STATE_RAWTEXT;
			case "script":
				return STATE_SCRIPT_DATA;
			case "plaintext":
				return STATE_PLAINTEXT;
			default:
				return STATE_DATA;
		}
	};

	/**
	 * @param {number} endPos end position
	 */
	const flushText = (endPos) => {
		if (textStart < endPos && callbacks.text !== undefined) {
			callbacks.text(input, textStart, endPos);
		}
	};

	/**
	 * @param {number} endPos end position
	 * @returns {number} next position
	 */
	const emitAttribute = (endPos) => {
		// Default `nextPos` advances past the closing quote (if any) so the
		// state machine can continue when no `attribute` callback is provided.
		// When a callback IS provided, its return value overrides the default —
		// the callback is expected to do the same advance based on the
		// reported `quoteType`.
		let nextPos = attrQuoteType === QUOTE_NONE ? endPos : endPos + 1;
		if (callbacks.attribute !== undefined && attrNameStart !== -1) {
			nextPos = callbacks.attribute(
				input,
				attrNameStart,
				attrNameEnd,
				attrValueStart,
				attrValueStart === -1 ? -1 : endPos,
				attrQuoteType
			);
		}
		attrNameStart = -1;
		attrValueStart = -1;
		attrQuoteType = QUOTE_NONE;
		return nextPos;
	};

	/**
	 * @param {number} endPos end position
	 * @param {boolean} selfClosing is self closing
	 * @returns {number} next position
	 */
	const emitOpenTag = (endPos, selfClosing) => {
		let nextPos = endPos;
		if (callbacks.openTag !== undefined) {
			nextPos = callbacks.openTag(
				input,
				tagStart,
				endPos,
				tagNameStart,
				tagNameEnd,
				selfClosing
			);
		}
		if (!selfClosing) {
			lastOpenTagName = input.slice(tagNameStart, tagNameEnd).toLowerCase();
		}
		textStart = nextPos;
		return nextPos;
	};

	/**
	 * @param {number} endPos end position
	 * @returns {number} next position
	 */
	const emitCloseTag = (endPos) => {
		let nextPos = endPos;
		if (callbacks.closeTag !== undefined) {
			nextPos = callbacks.closeTag(
				input,
				tagStart,
				endPos,
				tagNameStart,
				tagNameEnd
			);
		}
		textStart = nextPos;
		return nextPos;
	};

	while (pos < len) {
		const cc = input.charCodeAt(pos);

		// TODO: We don't handle all states here yet. In the future we will need to handle
		// all of them, and when we move all the tokenizer we will remove it.
		switch (state) {
			// https://html.spec.whatwg.org/multipage/parsing.html#data-state
			case STATE_DATA:
				// Consume the next input character:
				// U+003C LESS-THAN SIGN (<)
				// Set the return state to the data state. Switch to the tag open state.
				if (cc === CC_LESS_THAN) {
					tagStart = pos;
					state = STATE_TAG_OPEN;
					pos++;
				} else if (cc === CC_AMPERSAND) {
					// U+0026 AMPERSAND (&)
					// Set the return state to the data state. Switch to the
					// character reference state.
					returnState = STATE_DATA;
					state = STATE_CHARACTER_REFERENCE;
					pos++;
				} else {
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#tag-open-state
			case STATE_TAG_OPEN:
				// Consume the next input character:
				// U+002F SOLIDUS (/)
				// Switch to the end tag open state.
				if (cc === CC_SOLIDUS) {
					state = STATE_END_TAG_OPEN;
					pos++;
				} else if (cc === CC_EXCLAMATION_MARK) {
					// U+0021 EXCLAMATION MARK (!)
					// Switch to the markup declaration open state.
					flushText(tagStart);
					commentStart = tagStart;
					state = STATE_MARKUP_DECLARATION_OPEN;
					pos++;
				} else if (isAsciiAlpha(cc)) {
					// ASCII alpha
					// Create a new start tag token, set its tag name to the empty string.
					// Reconsume in the tag name state.
					flushText(tagStart);
					tagNameStart = pos;
					state = STATE_TAG_NAME;
					// Reconsume
				} else if (cc === CC_QUESTION_MARK) {
					// U+003F QUESTION MARK (?)
					// This is an unexpected-question-mark-instead-of-tag-name parse error.
					// Create a comment token whose data is the empty string. Reconsume in the
					// bogus comment state.
					reportError(
						"unexpected-question-mark-instead-of-tag-name",
						pos,
						pos + 1,
						"warning"
					);
					flushText(tagStart);
					commentStart = tagStart;
					state = STATE_BOGUS_COMMENT;
					// Reconsume — let the bogus-comment state consume the `?`
					// itself, matching the spec.
				} else {
					// Anything else
					// This is an invalid-first-character-of-tag-name parse error. Emit a U+003C
					// LESS-THAN SIGN character token. Reconsume in the data state.
					reportError(
						"invalid-first-character-of-tag-name",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_DATA;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#end-tag-open-state
			case STATE_END_TAG_OPEN:
				// Consume the next input character:
				// ASCII alpha
				// Create a new end tag token, set its tag name to the empty string.
				// Reconsume in the tag name state.
				if (isAsciiAlpha(cc)) {
					flushText(tagStart);
					tagNameStart = pos;
					state = STATE_TAG_NAME;
					// Reconsume
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// This is a missing-end-tag-name parse error. Switch to the data state.
					reportError("missing-end-tag-name", pos, pos + 1, "warning");
					state = STATE_DATA;
					pos++;
				} else {
					// Anything else
					// This is an invalid-first-character-of-tag-name parse error. Create a
					// comment token whose data is the empty string. Reconsume in the bogus
					// comment state.
					reportError(
						"invalid-first-character-of-tag-name",
						pos,
						pos + 1,
						"warning"
					);
					flushText(tagStart);
					commentStart = tagStart;
					state = STATE_BOGUS_COMMENT;
					// Reconsume — let bogus-comment consume this char itself.
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#tag-name-state
			case STATE_TAG_NAME:
				// Consume the next input character:
				// U+0009 CHARACTER TABULATION (tab)
				// U+000A LINE FEED (LF)
				// U+000C FORM FEED (FF)
				// U+0020 SPACE
				// Switch to the before attribute name state.
				if (isSpace(cc)) {
					tagNameEnd = pos;
					state = STATE_BEFORE_ATTRIBUTE_NAME;
					pos++;
				} else if (cc === CC_SOLIDUS) {
					// U+002F SOLIDUS (/)
					// Switch to the self-closing start tag state.
					tagNameEnd = pos;
					state = STATE_SELF_CLOSING_START_TAG;
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// Switch to the data state. Emit the current tag token.
					tagNameEnd = pos;
					if (input.charCodeAt(tagStart + 1) === CC_SOLIDUS) {
						state = STATE_DATA;
						pos = emitCloseTag(pos + 1);
					} else {
						const nextPos = emitOpenTag(pos + 1, false);
						state =
							nextPos > pos + 1
								? STATE_DATA
								: getContentModeForTag(lastOpenTagName);
						pos = nextPos;
					}
				} else {
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#before-attribute-name-state
			case STATE_BEFORE_ATTRIBUTE_NAME:
				// Consume the next input character:
				// U+0009 CHARACTER TABULATION (tab)
				// U+000A LINE FEED (LF)
				// U+000C FORM FEED (FF)
				// U+0020 SPACE
				// Ignore the character.
				// Reconsume so space is handled in BEFORE_ATTRIBUTE_NAME
				if (isSpace(cc)) {
					pos++;
				} else if (cc === CC_SOLIDUS || cc === CC_GREATER_THAN) {
					// U+002F SOLIDUS (/)
					// U+003E GREATER-THAN SIGN (>)
					// EOF
					// Reconsume in the after attribute name state.
					state = STATE_AFTER_ATTRIBUTE_NAME;
					// Reconsume
				} else if (cc === CC_EQUALS) {
					// U+003D EQUALS SIGN (=)
					// This is an unexpected-equals-sign-before-attribute-name parse
					// error. Start a new attribute. Switch to the attribute name state.
					reportError(
						"unexpected-equals-sign-before-attribute-name",
						pos,
						pos + 1,
						"warning"
					);
					attrNameStart = pos;
					state = STATE_ATTRIBUTE_NAME;
					pos++;
				} else {
					// Anything else
					// Start a new attribute in the current tag token. Set that attribute name
					// and value to the empty string. Reconsume in the attribute name state.
					attrNameStart = pos;
					state = STATE_ATTRIBUTE_NAME;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#attribute-name-state
			case STATE_ATTRIBUTE_NAME:
				// Consume the next input character:
				// U+0009 CHARACTER TABULATION (tab)
				// U+000A LINE FEED (LF)
				// U+000C FORM FEED (FF)
				// U+0020 SPACE
				// U+002F SOLIDUS (/)
				// U+003E GREATER-THAN SIGN (>)
				// EOF
				// Reconsume in the after attribute name state.
				if (isSpace(cc) || cc === CC_SOLIDUS || cc === CC_GREATER_THAN) {
					attrNameEnd = pos;
					state = STATE_AFTER_ATTRIBUTE_NAME;
					// Reconsume
				} else if (cc === CC_EQUALS) {
					attrNameEnd = pos;
					state = STATE_BEFORE_ATTRIBUTE_VALUE;
					pos++;
				} else {
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#after-attribute-name-state
			case STATE_AFTER_ATTRIBUTE_NAME:
				// Consume the next input character:
				// U+0009 CHARACTER TABULATION (tab)
				// U+000A LINE FEED (LF)
				// U+000C FORM FEED (FF)
				// U+0020 SPACE
				// Ignore the character.
				if (isSpace(cc)) {
					pos++;
				} else if (cc === CC_SOLIDUS) {
					// U+002F SOLIDUS (/)
					// Switch to the self-closing start tag state.
					emitAttribute(pos);
					state = STATE_SELF_CLOSING_START_TAG;
					pos++;
				} else if (cc === CC_EQUALS) {
					// U+003D EQUALS SIGN (=)
					// Switch to the before attribute value state.
					state = STATE_BEFORE_ATTRIBUTE_VALUE;
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// Switch to the data state. Emit the current tag token.
					emitAttribute(pos);
					if (input.charCodeAt(tagStart + 1) === CC_SOLIDUS) {
						state = STATE_DATA;
						pos = emitCloseTag(pos + 1);
					} else {
						const nextPos = emitOpenTag(pos + 1, false);
						state =
							nextPos > pos + 1
								? STATE_DATA
								: getContentModeForTag(lastOpenTagName);
						pos = nextPos;
					}
				} else {
					// Anything else
					// Start a new attribute in the current tag token.
					emitAttribute(pos);
					attrNameStart = pos;
					state = STATE_ATTRIBUTE_NAME;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#before-attribute-value-state
			case STATE_BEFORE_ATTRIBUTE_VALUE:
				// Consume the next input character:
				// U+0009 CHARACTER TABULATION (tab)
				// U+000A LINE FEED (LF)
				// U+000C FORM FEED (FF)
				// U+0020 SPACE
				// Ignore the character.
				if (isSpace(cc)) {
					pos++;
				} else if (cc === CC_QUOTATION_MARK) {
					// U+0022 QUOTATION MARK (")
					// Switch to the attribute value (double-quoted) state.
					attrValueStart = pos + 1;
					attrQuoteType = QUOTE_DOUBLE;
					state = STATE_ATTRIBUTE_VALUE_DOUBLE_QUOTED;
					pos++;
				} else if (cc === CC_APOSTROPHE) {
					// U+0027 APOSTROPHE (')
					// Switch to the attribute value (single-quoted) state.
					attrValueStart = pos + 1;
					attrQuoteType = QUOTE_SINGLE;
					state = STATE_ATTRIBUTE_VALUE_SINGLE_QUOTED;
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// This is a missing-attribute-value parse error. Switch to the data
					// state. Emit the current tag token. The attribute is reported with
					// an empty value range pointing at the `>` so the open-tag byte range
					// still includes the `>`.
					reportError("missing-attribute-value", pos, pos + 1, "warning");
					attrValueStart = pos;
					attrQuoteType = QUOTE_NONE;
					pos = emitAttribute(pos);
					if (input.charCodeAt(tagStart + 1) === CC_SOLIDUS) {
						state = STATE_DATA;
						pos = emitCloseTag(pos + 1);
					} else {
						const nextPos = emitOpenTag(pos + 1, false);
						state =
							nextPos > pos + 1
								? STATE_DATA
								: getContentModeForTag(lastOpenTagName);
						pos = nextPos;
					}
				} else {
					// Anything else
					// Reconsume in the attribute value (unquoted) state.
					attrValueStart = pos;
					attrQuoteType = QUOTE_NONE;
					state = STATE_ATTRIBUTE_VALUE_UNQUOTED;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#attribute-value-(double-quoted)-state
			case STATE_ATTRIBUTE_VALUE_DOUBLE_QUOTED:
				// Consume the next input character:
				// U+0022 QUOTATION MARK (")
				// Switch to the after attribute value (quoted) state.
				if (cc === CC_QUOTATION_MARK) {
					pos = emitAttribute(pos);
					state = STATE_AFTER_ATTRIBUTE_VALUE_QUOTED;
				} else if (cc === CC_AMPERSAND) {
					// U+0026 AMPERSAND (&)
					// Set the return state to the attribute value (double-quoted)
					// state. Switch to the character reference state.
					returnState = STATE_ATTRIBUTE_VALUE_DOUBLE_QUOTED;
					state = STATE_CHARACTER_REFERENCE;
					pos++;
				} else {
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#attribute-value-(single-quoted)-state
			case STATE_ATTRIBUTE_VALUE_SINGLE_QUOTED:
				// Consume the next input character:
				// U+0027 APOSTROPHE (')
				// Switch to the after attribute value (quoted) state.
				if (cc === CC_APOSTROPHE) {
					pos = emitAttribute(pos);
					state = STATE_AFTER_ATTRIBUTE_VALUE_QUOTED;
				} else if (cc === CC_AMPERSAND) {
					// U+0026 AMPERSAND (&)
					// Set the return state to the attribute value (single-quoted)
					// state. Switch to the character reference state.
					returnState = STATE_ATTRIBUTE_VALUE_SINGLE_QUOTED;
					state = STATE_CHARACTER_REFERENCE;
					pos++;
				} else {
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#attribute-value-(unquoted)-state
			case STATE_ATTRIBUTE_VALUE_UNQUOTED:
				if (isSpace(cc)) {
					pos = emitAttribute(pos);
					state = STATE_BEFORE_ATTRIBUTE_NAME;
					// Reconsume so space is handled in BEFORE_ATTRIBUTE_NAME
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// This is a missing-attribute-value parse error. Switch to the data state.
					// Emit the current tag token.
					pos = emitAttribute(pos);
					if (input.charCodeAt(tagStart + 1) === CC_SOLIDUS) {
						state = STATE_DATA;
						pos = emitCloseTag(pos + 1);
					} else {
						const nextPos = emitOpenTag(pos + 1, false);
						state =
							nextPos > pos + 1
								? STATE_DATA
								: getContentModeForTag(lastOpenTagName);
						pos = nextPos;
					}
				} else if (cc === CC_AMPERSAND) {
					// U+0026 AMPERSAND (&)
					// Set the return state to the attribute value (unquoted)
					// state. Switch to the character reference state.
					returnState = STATE_ATTRIBUTE_VALUE_UNQUOTED;
					state = STATE_CHARACTER_REFERENCE;
					pos++;
				} else {
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#after-attribute-value-(quoted)-state
			case STATE_AFTER_ATTRIBUTE_VALUE_QUOTED:
				// Consume the next input character:
				// U+0009 CHARACTER TABULATION (tab)
				// U+000A LINE FEED (LF)
				// U+000C FORM FEED (FF)
				// U+0020 SPACE
				// Switch to the before attribute name state.
				if (isSpace(cc)) {
					state = STATE_BEFORE_ATTRIBUTE_NAME;
					pos++;
				} else if (cc === CC_SOLIDUS) {
					// U+002F SOLIDUS (/)
					// Switch to the self-closing start tag state.
					state = STATE_SELF_CLOSING_START_TAG;
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					if (input.charCodeAt(tagStart + 1) === CC_SOLIDUS) {
						state = STATE_DATA;
						pos = emitCloseTag(pos + 1);
					} else {
						const nextPos = emitOpenTag(pos + 1, false);
						state =
							nextPos > pos + 1
								? STATE_DATA
								: getContentModeForTag(lastOpenTagName);
						pos = nextPos;
					}
				} else {
					// Anything else
					// This is a missing-whitespace-between-attributes parse error. Reconsume in
					// the before attribute name state.
					reportError(
						"missing-whitespace-between-attributes",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_BEFORE_ATTRIBUTE_NAME;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#self-closing-start-tag-state
			case STATE_SELF_CLOSING_START_TAG:
				// Consume the next input character:
				// U+003E GREATER-THAN SIGN (>)
				// Set the self-closing flag of the current tag token. Switch to the data
				// state. Emit the current tag token.
				if (cc === CC_GREATER_THAN) {
					if (input.charCodeAt(tagStart + 1) === CC_SOLIDUS) {
						state = STATE_DATA;
						pos = emitCloseTag(pos + 1);
					} else {
						pos = emitOpenTag(pos + 1, true);
						state = STATE_DATA;
					}
				} else {
					// Anything else
					// This is an unexpected-solidus-in-tag parse error. Reconsume in the before
					// attribute name state.
					reportError("unexpected-solidus-in-tag", pos, pos + 1, "warning");
					state = STATE_BEFORE_ATTRIBUTE_NAME;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#markup-declaration-open-state
			case STATE_MARKUP_DECLARATION_OPEN:
				// If the next few characters are:
				// Two U+002D HYPHEN-MINUS characters (-)
				// Consume those two characters, create a comment token whose data
				// is the empty string, and switch to the comment start state.
				if (
					cc === CC_HYPHEN_MINUS &&
					input.charCodeAt(pos + 1) === CC_HYPHEN_MINUS
				) {
					pos += 2;
					commentStart = tagStart;
					state = STATE_COMMENT_START;
				} else if (
					// ASCII case-insensitive match for the word "DOCTYPE"
					// Consume those characters and switch to the DOCTYPE state.
					(cc === 0x44 || cc === 0x64) /* D or d */ &&
					(input.charCodeAt(pos + 1) | 0x20) === 0x6f /* o */ &&
					(input.charCodeAt(pos + 2) | 0x20) === 0x63 /* c */ &&
					(input.charCodeAt(pos + 3) | 0x20) === 0x74 /* t */ &&
					(input.charCodeAt(pos + 4) | 0x20) === 0x79 /* y */ &&
					(input.charCodeAt(pos + 5) | 0x20) === 0x70 /* p */ &&
					(input.charCodeAt(pos + 6) | 0x20) === 0x65 /* e */
				) {
					pos += 7;
					commentStart = tagStart;
					state = STATE_DOCTYPE;
				} else if (
					// The string "[CDATA[" (the five uppercase letters "CDATA" with a
					// U+005B LEFT SQUARE BRACKET character before and after)
					// Consume those characters and switch to the CDATA section state.
					cc === CC_LEFT_SQUARE_BRACKET &&
					input.charCodeAt(pos + 1) === 0x43 /* C */ &&
					input.charCodeAt(pos + 2) === 0x44 /* D */ &&
					input.charCodeAt(pos + 3) === 0x41 /* A */ &&
					input.charCodeAt(pos + 4) === 0x54 /* T */ &&
					input.charCodeAt(pos + 5) === 0x41 /* A */ &&
					input.charCodeAt(pos + 6) === CC_LEFT_SQUARE_BRACKET
				) {
					pos += 7;
					commentStart = tagStart;
					state = STATE_CDATA_SECTION;
				} else {
					// Anything else
					// This is an incorrectly-opened-comment parse error. Create a comment token
					// whose data is the empty string. Switch to the bogus comment state (don't
					// consume anything in the current state).
					reportError("incorrectly-opened-comment", tagStart, pos, "warning");
					commentStart = tagStart;
					state = STATE_BOGUS_COMMENT;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#comment-start-state
			case STATE_COMMENT_START:
				// Consume the next input character:
				// U+002D HYPHEN-MINUS (-)
				// Switch to the comment start dash state.
				if (cc === CC_HYPHEN_MINUS) {
					state = STATE_COMMENT_START_DASH;
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// This is an abrupt-closing-of-empty-comment parse error. Switch to the
					// data state. Emit the current comment token.
					reportError(
						"abrupt-closing-of-empty-comment",
						pos,
						pos + 1,
						"warning"
					);
					let nextPos = pos + 1;
					if (callbacks.comment !== undefined) {
						nextPos = callbacks.comment(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else {
					// Anything else
					// Reconsume in the comment state.
					state = STATE_COMMENT;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#comment-start-dash-state
			case STATE_COMMENT_START_DASH:
				// Consume the next input character:
				// U+002D HYPHEN-MINUS (-)
				// Switch to the comment end state.
				if (cc === CC_HYPHEN_MINUS) {
					state = STATE_COMMENT_END;
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// This is an abrupt-closing-of-empty-comment parse error. Switch to the
					// data state. Emit the current comment token.
					reportError(
						"abrupt-closing-of-empty-comment",
						pos,
						pos + 1,
						"warning"
					);
					let nextPos = pos + 1;
					if (callbacks.comment !== undefined) {
						nextPos = callbacks.comment(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else {
					// Anything else
					// Append a U+002D HYPHEN-MINUS character (-) to the comment token's data.
					// Reconsume in the comment state.
					state = STATE_COMMENT;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#comment-state
			case STATE_COMMENT:
				// Consume the next input character:
				// U+003C LESS-THAN SIGN (<)
				// Append a U+003C LESS-THAN SIGN character to the comment token's data. Switch to the comment less-than sign state.
				if (cc === CC_LESS_THAN) {
					state = STATE_COMMENT_LESS_THAN_SIGN;
					pos++;
				} else if (cc === CC_HYPHEN_MINUS) {
					// Consume the next input character:
					// U+002D HYPHEN-MINUS (-)
					// Switch to the comment end dash state.
					state = STATE_COMMENT_END_DASH;
					pos++;
				} else {
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#comment-end-dash-state
			case STATE_COMMENT_END_DASH:
				// Consume the next input character:
				// U+002D HYPHEN-MINUS (-)
				// Switch to the comment end state.
				if (cc === CC_HYPHEN_MINUS) {
					state = STATE_COMMENT_END;
					pos++;
				} else {
					// Anything else
					// Append a U+002D HYPHEN-MINUS character (-) to the comment token's data.
					// Reconsume in the comment state.
					state = STATE_COMMENT;
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#comment-end-state
			case STATE_COMMENT_END:
				// Consume the next input character:
				// U+003E GREATER-THAN SIGN (>)
				// Switch to the data state. Emit the current comment token.
				if (cc === CC_GREATER_THAN) {
					let nextPos = pos + 1;
					if (callbacks.comment !== undefined) {
						nextPos = callbacks.comment(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else if (cc === CC_EXCLAMATION_MARK) {
					// U+0021 EXCLAMATION MARK (!)
					// Switch to the comment end bang state.
					state = STATE_COMMENT_END_BANG;
					pos++;
				} else if (cc === CC_HYPHEN_MINUS) {
					pos++;
				} else {
					// Anything else
					// Append two U+002D HYPHEN-MINUS characters (-) to the comment token's
					// data. Reconsume in the comment state.
					state = STATE_COMMENT;
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#comment-end-bang-state
			case STATE_COMMENT_END_BANG:
				// Consume the next input character:
				// U+002D HYPHEN-MINUS (-)
				// Append two U+002D HYPHEN-MINUS characters (-) and a U+0021 EXCLAMATION
				// MARK character (!) to the comment token's data. Switch to the comment end
				// dash state.
				if (cc === CC_HYPHEN_MINUS) {
					state = STATE_COMMENT_END_DASH;
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// This is an incorrectly-closed-comment parse error. Switch to the data
					// state. Emit the current comment token.
					reportError("incorrectly-closed-comment", pos, pos + 1, "warning");
					let nextPos = pos + 1;
					if (callbacks.comment !== undefined) {
						nextPos = callbacks.comment(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else {
					// Anything else
					// Append two U+002D HYPHEN-MINUS characters (-) and a U+0021 EXCLAMATION
					// MARK character (!) to the comment token's data. Reconsume in the comment
					// state.
					state = STATE_COMMENT;
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#bogus-comment-state
			case STATE_BOGUS_COMMENT:
				// Consume the next input character:
				// U+003E GREATER-THAN SIGN (>)
				// Switch to the data state. Emit the current comment token.
				if (cc === CC_GREATER_THAN) {
					let nextPos = pos + 1;
					if (callbacks.comment !== undefined) {
						nextPos = callbacks.comment(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else {
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#comment-less-than-sign-state
			case STATE_COMMENT_LESS_THAN_SIGN:
				// Consume the next input character:
				// U+0021 EXCLAMATION MARK (!)
				// Append the current input character to the comment token's data. Switch to
				// the comment less-than sign bang state.
				if (cc === CC_EXCLAMATION_MARK) {
					state = STATE_COMMENT_LESS_THAN_SIGN_BANG;
					pos++;
				} else if (cc === CC_LESS_THAN) {
					// U+003C LESS-THAN SIGN (<)
					// Append the current input character to the comment token's data.
					pos++;
				} else {
					// Anything else
					// Reconsume in the comment state.
					state = STATE_COMMENT;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#comment-less-than-sign-bang-state
			case STATE_COMMENT_LESS_THAN_SIGN_BANG:
				// Consume the next input character:
				// U+002D HYPHEN-MINUS (-)
				// Switch to the comment less-than sign bang dash state.
				if (cc === CC_HYPHEN_MINUS) {
					state = STATE_COMMENT_LESS_THAN_SIGN_BANG_DASH;
					pos++;
				} else {
					// Anything else
					// Reconsume in the comment state.
					state = STATE_COMMENT;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#comment-less-than-sign-bang-dash-state
			case STATE_COMMENT_LESS_THAN_SIGN_BANG_DASH:
				// Consume the next input character:
				// U+002D HYPHEN-MINUS (-)
				// Switch to the comment less-than sign bang dash dash state.
				if (cc === CC_HYPHEN_MINUS) {
					state = STATE_COMMENT_LESS_THAN_SIGN_BANG_DASH_DASH;
					pos++;
				} else {
					// Anything else
					// Reconsume in the comment end dash state.
					state = STATE_COMMENT_END_DASH;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#comment-less-than-sign-bang-dash-dash-state
			case STATE_COMMENT_LESS_THAN_SIGN_BANG_DASH_DASH:
				// Consume the next input character:
				// U+003E GREATER-THAN SIGN (>)
				// EOF
				// Reconsume in the comment end state.
				// Anything else
				// This is a nested-comment parse error. Reconsume in the comment end state.
				if (cc !== CC_GREATER_THAN) {
					reportError("nested-comment", pos, pos + 1, "warning");
				}
				state = STATE_COMMENT_END;
				// Reconsume
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#doctype-state
			case STATE_DOCTYPE:
				// Consume the next input character:
				// U+0009 CHARACTER TABULATION (tab)
				// U+000A LINE FEED (LF)
				// U+000C FORM FEED (FF)
				// U+0020 SPACE
				// Switch to the before DOCTYPE name state.
				if (isSpace(cc)) {
					state = STATE_BEFORE_DOCTYPE_NAME;
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// Reconsume in the before DOCTYPE name state.
					state = STATE_BEFORE_DOCTYPE_NAME;
				} else {
					// Anything else
					// This is a missing-whitespace-before-doctype-name parse error. Reconsume
					// in the before DOCTYPE name state.
					reportError(
						"missing-whitespace-before-doctype-name",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_BEFORE_DOCTYPE_NAME;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#before-doctype-name-state
			case STATE_BEFORE_DOCTYPE_NAME:
				// Consume the next input character:
				// U+0009 CHARACTER TABULATION (tab)
				// U+000A LINE FEED (LF)
				// U+000C FORM FEED (FF)
				// U+0020 SPACE
				// Ignore the character.
				if (isSpace(cc)) {
					pos++;
				} else if (cc === 0x00) {
					// U+0000 NULL
					// This is an unexpected-null-character parse error. Create a new DOCTYPE
					// token. Set the token's name to a U+FFFD REPLACEMENT CHARACTER character.
					// Switch to the DOCTYPE name state.
					state = STATE_DOCTYPE_NAME;
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// This is a missing-doctype-name parse error. Create a new DOCTYPE token.
					// Set its force-quirks flag to on. Switch to the data state. Emit the
					// current token.
					reportError("missing-doctype-name", pos, pos + 1, "warning");
					let nextPos = pos + 1;
					if (callbacks.doctype !== undefined) {
						nextPos = callbacks.doctype(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else {
					// ASCII upper alpha
					// Create a new DOCTYPE token. Set the token's name to the lowercase version
					// of the current input character (add 0x0020 to the character's code
					// point). Switch to the DOCTYPE name state.
					// Anything else
					// Create a new DOCTYPE token. Set the token's name to the current input
					// character. Switch to the DOCTYPE name state.
					state = STATE_DOCTYPE_NAME;
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#doctype-name-state
			case STATE_DOCTYPE_NAME:
				// Consume the next input character:
				// U+0009 CHARACTER TABULATION (tab)
				// U+000A LINE FEED (LF)
				// U+000C FORM FEED (FF)
				// U+0020 SPACE
				// Switch to the after DOCTYPE name state.
				if (isSpace(cc)) {
					state = STATE_AFTER_DOCTYPE_NAME;
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// Switch to the data state. Emit the current DOCTYPE token.
					let nextPos = pos + 1;
					if (callbacks.doctype !== undefined) {
						nextPos = callbacks.doctype(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else if (cc === 0x00) {
					// U+0000 NULL
					// This is an unexpected-null-character parse error. Append a U+FFFD
					// REPLACEMENT CHARACTER character to the current DOCTYPE token's name.
					pos++;
				} else {
					// ASCII upper alpha
					// Append the lowercase version of the current input character (add 0x0020
					// to the character's code point) to the current DOCTYPE token's name.
					// Anything else
					// Append the current input character to the current DOCTYPE token's name.
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#after-doctype-name-state
			case STATE_AFTER_DOCTYPE_NAME:
				// Consume the next input character:
				if (isSpace(cc)) {
					// U+0009 CHARACTER TABULATION (tab)
					// U+000A LINE FEED (LF)
					// U+000C FORM FEED (FF)
					// U+0020 SPACE
					// Ignore the character.
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// Switch to the data state. Emit the current DOCTYPE token.
					let nextPos = pos + 1;
					if (callbacks.doctype !== undefined) {
						nextPos = callbacks.doctype(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else if (
					pos + 5 < len &&
					(cc === 0x50 || cc === 0x70) /* P or p */ &&
					(input.charCodeAt(pos + 1) | 0x20) === 0x75 /* u */ &&
					(input.charCodeAt(pos + 2) | 0x20) === 0x62 /* b */ &&
					(input.charCodeAt(pos + 3) | 0x20) === 0x6c /* l */ &&
					(input.charCodeAt(pos + 4) | 0x20) === 0x69 /* i */ &&
					(input.charCodeAt(pos + 5) | 0x20) === 0x63 /* c */
				) {
					// ASCII case-insensitive match for the word "PUBLIC"
					pos += 6;
					state = STATE_AFTER_DOCTYPE_PUBLIC_KEYWORD;
				} else if (
					pos + 5 < len &&
					(cc === 0x53 || cc === 0x73) /* S or s */ &&
					(input.charCodeAt(pos + 1) | 0x20) === 0x79 /* y */ &&
					(input.charCodeAt(pos + 2) | 0x20) === 0x73 /* s */ &&
					(input.charCodeAt(pos + 3) | 0x20) === 0x74 /* t */ &&
					(input.charCodeAt(pos + 4) | 0x20) === 0x65 /* e */ &&
					(input.charCodeAt(pos + 5) | 0x20) === 0x6d /* m */
				) {
					// ASCII case-insensitive match for the word "SYSTEM"
					pos += 6;
					state = STATE_AFTER_DOCTYPE_SYSTEM_KEYWORD;
				} else {
					// Anything else
					// This is an invalid-character-sequence-after-doctype-name parse error. Set
					// the current DOCTYPE token's force-quirks flag to on. Reconsume in the
					// bogus DOCTYPE state.
					reportError(
						"invalid-character-sequence-after-doctype-name",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_BOGUS_DOCTYPE;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#after-doctype-public-keyword-state
			case STATE_AFTER_DOCTYPE_PUBLIC_KEYWORD:
				// Consume the next input character:
				if (isSpace(cc)) {
					// U+0009 CHARACTER TABULATION (tab)
					// U+000A LINE FEED (LF)
					// U+000C FORM FEED (FF)
					// U+0020 SPACE
					// Switch to the before DOCTYPE public identifier state.
					state = STATE_BEFORE_DOCTYPE_PUBLIC_IDENTIFIER;
					pos++;
				} else if (cc === CC_QUOTATION_MARK) {
					// U+0022 QUOTATION MARK (")
					// This is a missing-whitespace-after-doctype-public-keyword parse error.
					// Set the current DOCTYPE token's public identifier to the empty string
					// (not missing), then switch to the DOCTYPE public identifier
					// (double-quoted) state.
					reportError(
						"missing-whitespace-after-doctype-public-keyword",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_DOCTYPE_PUBLIC_IDENTIFIER_DOUBLE_QUOTED;
					pos++;
				} else if (cc === CC_APOSTROPHE) {
					// U+0027 APOSTROPHE (')
					// This is a missing-whitespace-after-doctype-public-keyword parse error.
					// Set the current DOCTYPE token's public identifier to the empty string
					// (not missing), then switch to the DOCTYPE public identifier
					// (single-quoted) state.
					reportError(
						"missing-whitespace-after-doctype-public-keyword",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_DOCTYPE_PUBLIC_IDENTIFIER_SINGLE_QUOTED;
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// This is a missing-doctype-public-identifier parse error. Set the current
					// DOCTYPE token's force-quirks flag to on. Switch to the data state. Emit
					// the current DOCTYPE token.
					reportError(
						"missing-doctype-public-identifier",
						pos,
						pos + 1,
						"warning"
					);
					let nextPos = pos + 1;
					if (callbacks.doctype !== undefined) {
						nextPos = callbacks.doctype(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else {
					// Anything else
					// This is a missing-quote-before-doctype-public-identifier parse error. Set
					// the current DOCTYPE token's force-quirks flag to on. Reconsume in the
					// bogus DOCTYPE state.
					reportError(
						"missing-quote-before-doctype-public-identifier",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_BOGUS_DOCTYPE;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#before-doctype-public-identifier-state
			case STATE_BEFORE_DOCTYPE_PUBLIC_IDENTIFIER:
				// Consume the next input character:
				if (isSpace(cc)) {
					// U+0009 CHARACTER TABULATION (tab)
					// U+000A LINE FEED (LF)
					// U+000C FORM FEED (FF)
					// U+0020 SPACE
					// Ignore the character.
					pos++;
				} else if (cc === CC_QUOTATION_MARK) {
					// U+0022 QUOTATION MARK (")
					// Set the current DOCTYPE token's public identifier to the empty string
					// (not missing), then switch to the DOCTYPE public identifier
					// (double-quoted) state.
					state = STATE_DOCTYPE_PUBLIC_IDENTIFIER_DOUBLE_QUOTED;
					pos++;
				} else if (cc === CC_APOSTROPHE) {
					// U+0027 APOSTROPHE (')
					// Set the current DOCTYPE token's public identifier to the empty string
					// (not missing), then switch to the DOCTYPE public identifier
					// (single-quoted) state.
					state = STATE_DOCTYPE_PUBLIC_IDENTIFIER_SINGLE_QUOTED;
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// This is a missing-doctype-public-identifier parse error. Set the current
					// DOCTYPE token's force-quirks flag to on. Switch to the data state. Emit
					// the current DOCTYPE token.
					reportError(
						"missing-doctype-public-identifier",
						pos,
						pos + 1,
						"warning"
					);
					let nextPos = pos + 1;
					if (callbacks.doctype !== undefined) {
						nextPos = callbacks.doctype(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else {
					// Anything else
					// This is a missing-quote-before-doctype-public-identifier parse error. Set
					// the current DOCTYPE token's force-quirks flag to on. Reconsume in the
					// bogus DOCTYPE state.
					reportError(
						"missing-quote-before-doctype-public-identifier",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_BOGUS_DOCTYPE;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#doctype-public-identifier-(double-quoted)-state
			case STATE_DOCTYPE_PUBLIC_IDENTIFIER_DOUBLE_QUOTED:
				// Consume the next input character:
				if (cc === CC_QUOTATION_MARK) {
					// U+0022 QUOTATION MARK (")
					// Switch to the after DOCTYPE public identifier state.
					state = STATE_AFTER_DOCTYPE_PUBLIC_IDENTIFIER;
					pos++;
				} else if (cc === 0x00) {
					// U+0000 NULL
					// This is an unexpected-null-character parse error. Append a U+FFFD
					// REPLACEMENT CHARACTER character to the current DOCTYPE token's public
					// identifier.
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// This is an abrupt-doctype-public-identifier parse error. Set the current
					// DOCTYPE token's force-quirks flag to on. Switch to the data state. Emit
					// the current DOCTYPE token.
					reportError(
						"abrupt-doctype-public-identifier",
						pos,
						pos + 1,
						"warning"
					);
					let nextPos = pos + 1;
					if (callbacks.doctype !== undefined) {
						nextPos = callbacks.doctype(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else {
					// Anything else
					// Append the current input character to the current DOCTYPE token's public
					// identifier.
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#doctype-public-identifier-(single-quoted)-state
			case STATE_DOCTYPE_PUBLIC_IDENTIFIER_SINGLE_QUOTED:
				// Consume the next input character:
				if (cc === CC_APOSTROPHE) {
					// U+0027 APOSTROPHE (')
					// Switch to the after DOCTYPE public identifier state.
					state = STATE_AFTER_DOCTYPE_PUBLIC_IDENTIFIER;
					pos++;
				} else if (cc === 0x00) {
					// U+0000 NULL
					// This is an unexpected-null-character parse error. Append a U+FFFD
					// REPLACEMENT CHARACTER character to the current DOCTYPE token's public
					// identifier.
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// This is an abrupt-doctype-public-identifier parse error. Set the current
					// DOCTYPE token's force-quirks flag to on. Switch to the data state. Emit
					// the current DOCTYPE token.
					reportError(
						"abrupt-doctype-public-identifier",
						pos,
						pos + 1,
						"warning"
					);
					let nextPos = pos + 1;
					if (callbacks.doctype !== undefined) {
						nextPos = callbacks.doctype(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else {
					// Anything else
					// Append the current input character to the current DOCTYPE token's public
					// identifier.
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#after-doctype-public-identifier-state
			case STATE_AFTER_DOCTYPE_PUBLIC_IDENTIFIER:
				// Consume the next input character:
				if (isSpace(cc)) {
					// U+0009 CHARACTER TABULATION (tab)
					// U+000A LINE FEED (LF)
					// U+000C FORM FEED (FF)
					// U+0020 SPACE
					// Switch to the between DOCTYPE public and system identifiers state.
					state = STATE_BETWEEN_DOCTYPE_PUBLIC_AND_SYSTEM_IDENTIFIERS;
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// Switch to the data state. Emit the current DOCTYPE token.
					let nextPos = pos + 1;
					if (callbacks.doctype !== undefined) {
						nextPos = callbacks.doctype(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else if (cc === CC_QUOTATION_MARK) {
					// U+0022 QUOTATION MARK (")
					// This is a missing-whitespace-between-doctype-public-and-system-identifiers
					// parse error. Set the current DOCTYPE token's system
					// identifier to the empty string (not missing), then switch
					// to the DOCTYPE system identifier (double-quoted) state.
					reportError(
						"missing-whitespace-between-doctype-public-and-system-identifiers",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_DOCTYPE_SYSTEM_IDENTIFIER_DOUBLE_QUOTED;
					pos++;
				} else if (cc === CC_APOSTROPHE) {
					// U+0027 APOSTROPHE (')
					// This is a missing-whitespace-between-doctype-public-and-system-identifiers
					// parse error. Set the current DOCTYPE token's system
					// identifier to the empty string (not missing), then switch
					// to the DOCTYPE system identifier (single-quoted) state.
					reportError(
						"missing-whitespace-between-doctype-public-and-system-identifiers",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_DOCTYPE_SYSTEM_IDENTIFIER_SINGLE_QUOTED;
					pos++;
				} else {
					// Anything else
					// This is a missing-quote-before-doctype-system-identifier parse error. Set
					// the current DOCTYPE token's force-quirks flag to on. Reconsume in the
					// bogus DOCTYPE state.
					reportError(
						"missing-quote-before-doctype-system-identifier",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_BOGUS_DOCTYPE;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#between-doctype-public-and-system-identifiers-state
			case STATE_BETWEEN_DOCTYPE_PUBLIC_AND_SYSTEM_IDENTIFIERS:
				// Consume the next input character:
				if (isSpace(cc)) {
					// U+0009 CHARACTER TABULATION (tab)
					// U+000A LINE FEED (LF)
					// U+000C FORM FEED (FF)
					// U+0020 SPACE
					// Ignore the character.
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// Switch to the data state. Emit the current DOCTYPE token.
					let nextPos = pos + 1;
					if (callbacks.doctype !== undefined) {
						nextPos = callbacks.doctype(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else if (cc === CC_QUOTATION_MARK) {
					// U+0022 QUOTATION MARK (")
					// Set the current DOCTYPE token's system identifier to the empty string
					// (not missing), then switch to the DOCTYPE system identifier
					// (double-quoted) state.
					state = STATE_DOCTYPE_SYSTEM_IDENTIFIER_DOUBLE_QUOTED;
					pos++;
				} else if (cc === CC_APOSTROPHE) {
					// U+0027 APOSTROPHE (')
					// Set the current DOCTYPE token's system identifier to the empty string
					// (not missing), then switch to the DOCTYPE system identifier
					// (single-quoted) state.
					state = STATE_DOCTYPE_SYSTEM_IDENTIFIER_SINGLE_QUOTED;
					pos++;
				} else {
					// Anything else
					// This is a missing-quote-before-doctype-system-identifier parse error. Set
					// the current DOCTYPE token's force-quirks flag to on. Reconsume in the
					// bogus DOCTYPE state.
					reportError(
						"missing-quote-before-doctype-system-identifier",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_BOGUS_DOCTYPE;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#after-doctype-system-keyword-state
			case STATE_AFTER_DOCTYPE_SYSTEM_KEYWORD:
				// Consume the next input character:
				if (isSpace(cc)) {
					// U+0009 CHARACTER TABULATION (tab)
					// U+000A LINE FEED (LF)
					// U+000C FORM FEED (FF)
					// U+0020 SPACE
					// Switch to the before DOCTYPE system identifier state.
					state = STATE_BEFORE_DOCTYPE_SYSTEM_IDENTIFIER;
					pos++;
				} else if (cc === CC_QUOTATION_MARK) {
					// U+0022 QUOTATION MARK (")
					// This is a missing-whitespace-after-doctype-system-keyword parse error.
					// Set the current DOCTYPE token's system identifier to the empty string
					// (not missing), then switch to the DOCTYPE system identifier
					// (double-quoted) state.
					reportError(
						"missing-whitespace-after-doctype-system-keyword",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_DOCTYPE_SYSTEM_IDENTIFIER_DOUBLE_QUOTED;
					pos++;
				} else if (cc === CC_APOSTROPHE) {
					// U+0027 APOSTROPHE (')
					// This is a missing-whitespace-after-doctype-system-keyword parse error.
					// Set the current DOCTYPE token's system identifier to the empty string
					// (not missing), then switch to the DOCTYPE system identifier
					// (single-quoted) state.
					reportError(
						"missing-whitespace-after-doctype-system-keyword",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_DOCTYPE_SYSTEM_IDENTIFIER_SINGLE_QUOTED;
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// This is a missing-doctype-system-identifier parse error. Set the current
					// DOCTYPE token's force-quirks flag to on. Switch to the data state. Emit
					// the current DOCTYPE token.
					reportError(
						"missing-doctype-system-identifier",
						pos,
						pos + 1,
						"warning"
					);
					let nextPos = pos + 1;
					if (callbacks.doctype !== undefined) {
						nextPos = callbacks.doctype(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else {
					// Anything else
					// This is a missing-quote-before-doctype-system-identifier parse error. Set
					// the current DOCTYPE token's force-quirks flag to on. Reconsume in the
					// bogus DOCTYPE state.
					reportError(
						"missing-quote-before-doctype-system-identifier",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_BOGUS_DOCTYPE;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#before-doctype-system-identifier-state
			case STATE_BEFORE_DOCTYPE_SYSTEM_IDENTIFIER:
				// Consume the next input character:
				if (isSpace(cc)) {
					// U+0009 CHARACTER TABULATION (tab)
					// U+000A LINE FEED (LF)
					// U+000C FORM FEED (FF)
					// U+0020 SPACE
					// Ignore the character.
					pos++;
				} else if (cc === CC_QUOTATION_MARK) {
					// U+0022 QUOTATION MARK (")
					// Set the current DOCTYPE token's system identifier to the empty string
					// (not missing), then switch to the DOCTYPE system identifier
					// (double-quoted) state.
					state = STATE_DOCTYPE_SYSTEM_IDENTIFIER_DOUBLE_QUOTED;
					pos++;
				} else if (cc === CC_APOSTROPHE) {
					// U+0027 APOSTROPHE (')
					// Set the current DOCTYPE token's system identifier to the empty string
					// (not missing), then switch to the DOCTYPE system identifier
					// (single-quoted) state.
					state = STATE_DOCTYPE_SYSTEM_IDENTIFIER_SINGLE_QUOTED;
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// This is a missing-doctype-system-identifier parse error. Set the current
					// DOCTYPE token's force-quirks flag to on. Switch to the data state. Emit
					// the current DOCTYPE token.
					reportError(
						"missing-doctype-system-identifier",
						pos,
						pos + 1,
						"warning"
					);
					let nextPos = pos + 1;
					if (callbacks.doctype !== undefined) {
						nextPos = callbacks.doctype(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else {
					// Anything else
					// This is a missing-quote-before-doctype-system-identifier parse error. Set
					// the current DOCTYPE token's force-quirks flag to on. Reconsume in the
					// bogus DOCTYPE state.
					reportError(
						"missing-quote-before-doctype-system-identifier",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_BOGUS_DOCTYPE;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#doctype-system-identifier-(double-quoted)-state
			case STATE_DOCTYPE_SYSTEM_IDENTIFIER_DOUBLE_QUOTED:
				// Consume the next input character:
				if (cc === CC_QUOTATION_MARK) {
					// U+0022 QUOTATION MARK (")
					// Switch to the after DOCTYPE system identifier state.
					state = STATE_AFTER_DOCTYPE_SYSTEM_IDENTIFIER;
					pos++;
				} else if (cc === 0x00) {
					// U+0000 NULL
					// This is an unexpected-null-character parse error. Append a U+FFFD
					// REPLACEMENT CHARACTER character to the current DOCTYPE token's system
					// identifier.
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// This is an abrupt-doctype-system-identifier parse error. Set the current
					// DOCTYPE token's force-quirks flag to on. Switch to the data state. Emit
					// the current DOCTYPE token.
					reportError(
						"abrupt-doctype-system-identifier",
						pos,
						pos + 1,
						"warning"
					);
					let nextPos = pos + 1;
					if (callbacks.doctype !== undefined) {
						nextPos = callbacks.doctype(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else {
					// Anything else
					// Append the current input character to the current DOCTYPE token's system
					// identifier.
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#doctype-system-identifier-(single-quoted)-state
			case STATE_DOCTYPE_SYSTEM_IDENTIFIER_SINGLE_QUOTED:
				// Consume the next input character:
				if (cc === CC_APOSTROPHE) {
					// U+0027 APOSTROPHE (')
					// Switch to the after DOCTYPE system identifier state.
					state = STATE_AFTER_DOCTYPE_SYSTEM_IDENTIFIER;
					pos++;
				} else if (cc === 0x00) {
					// U+0000 NULL
					// This is an unexpected-null-character parse error. Append a U+FFFD
					// REPLACEMENT CHARACTER character to the current DOCTYPE token's system
					// identifier.
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// This is an abrupt-doctype-system-identifier parse error. Set the current
					// DOCTYPE token's force-quirks flag to on. Switch to the data state. Emit
					// the current DOCTYPE token.
					reportError(
						"abrupt-doctype-system-identifier",
						pos,
						pos + 1,
						"warning"
					);
					let nextPos = pos + 1;
					if (callbacks.doctype !== undefined) {
						nextPos = callbacks.doctype(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else {
					// Anything else
					// Append the current input character to the current DOCTYPE token's system
					// identifier.
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#after-doctype-system-identifier-state
			case STATE_AFTER_DOCTYPE_SYSTEM_IDENTIFIER:
				// Consume the next input character:
				if (isSpace(cc)) {
					// U+0009 CHARACTER TABULATION (tab)
					// U+000A LINE FEED (LF)
					// U+000C FORM FEED (FF)
					// U+0020 SPACE
					// Ignore the character.
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// Switch to the data state. Emit the current DOCTYPE token.
					let nextPos = pos + 1;
					if (callbacks.doctype !== undefined) {
						nextPos = callbacks.doctype(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else {
					// Anything else
					// This is an unexpected-character-after-doctype-system-identifier parse
					// error. Reconsume in the bogus DOCTYPE state. (This does not set the
					// current DOCTYPE token's force-quirks flag to on.)
					reportError(
						"unexpected-character-after-doctype-system-identifier",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_BOGUS_DOCTYPE;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#bogus-doctype-state
			case STATE_BOGUS_DOCTYPE:
				// Consume the next input character:
				if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// Switch to the data state. Emit the DOCTYPE token.
					let nextPos = pos + 1;
					if (callbacks.doctype !== undefined) {
						nextPos = callbacks.doctype(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else if (cc === 0x00) {
					// U+0000 NULL
					// This is an unexpected-null-character parse error. Ignore the character.
					pos++;
				} else {
					// Anything else
					// Ignore the character.
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#cdata-section-state
			case STATE_CDATA_SECTION:
				// Consume the next input character:
				// U+005D RIGHT SQUARE BRACKET (])
				// Switch to the CDATA section bracket state.
				if (cc === CC_RIGHT_SQUARE_BRACKET) {
					state = STATE_CDATA_SECTION_BRACKET;
					pos++;
				} else {
					// Anything else
					// Emit the current input character as a character token.
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#cdata-section-bracket-state
			case STATE_CDATA_SECTION_BRACKET:
				// Consume the next input character:
				// U+005D RIGHT SQUARE BRACKET (])
				// Switch to the CDATA section end state.
				if (cc === CC_RIGHT_SQUARE_BRACKET) {
					state = STATE_CDATA_SECTION_END;
					pos++;
				} else {
					// Anything else
					// Emit a U+005D RIGHT SQUARE BRACKET character token. Reconsume in the
					// CDATA section state.
					state = STATE_CDATA_SECTION;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#cdata-section-end-state
			case STATE_CDATA_SECTION_END:
				// Consume the next input character:
				// U+005D RIGHT SQUARE BRACKET (])
				// Emit a U+005D RIGHT SQUARE BRACKET character token.
				if (cc === CC_RIGHT_SQUARE_BRACKET) {
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// Switch to the data state.
					let nextPos = pos + 1;
					if (callbacks.comment !== undefined) {
						nextPos = callbacks.comment(input, commentStart, pos + 1);
					}
					state = STATE_DATA;
					textStart = nextPos;
					pos = nextPos;
				} else {
					// Anything else
					// Emit two U+005D RIGHT SQUARE BRACKET character tokens. Reconsume in the
					// CDATA section state.
					state = STATE_CDATA_SECTION;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#rcdata-state
			case STATE_RCDATA:
				// Consume the next input character:
				// U+003C LESS-THAN SIGN (<)
				// Switch to the RCDATA less-than sign state.
				if (cc === CC_LESS_THAN) {
					tagStart = pos;
					state = STATE_RCDATA_LESS_THAN_SIGN;
					pos++;
				} else {
					// Anything else
					// Emit the current input character as a character token.
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#rcdata-less-than-sign-state
			case STATE_RCDATA_LESS_THAN_SIGN:
				// Consume the next input character:
				// U+002F SOLIDUS (/)
				// Set the temporary buffer to the empty string. Switch to the RCDATA end
				// tag open state.
				if (cc === CC_SOLIDUS) {
					tempBuffer = "";
					state = STATE_RCDATA_END_TAG_OPEN;
					pos++;
				} else {
					// Anything else
					// Emit a U+003C LESS-THAN SIGN character token. Reconsume in the RCDATA
					// state.
					state = STATE_RCDATA;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#rcdata-end-tag-open-state
			case STATE_RCDATA_END_TAG_OPEN:
				// Consume the next input character:
				// ASCII alpha
				// Create a new end tag token, set its tag name to the empty string.
				// Reconsume in the RCDATA end tag name state.
				if (isAsciiAlpha(cc)) {
					tagNameStart = pos;
					state = STATE_RCDATA_END_TAG_NAME;
					// Reconsume
				} else {
					// Anything else
					// Emit a U+003C LESS-THAN SIGN character token and a U+002F SOLIDUS
					// character token. Reconsume in the RCDATA state.
					state = STATE_RCDATA;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#rcdata-end-tag-name-state
			case STATE_RCDATA_END_TAG_NAME:
				// Consume the next input character:
				// U+0009 CHARACTER TABULATION (tab)
				// U+000A LINE FEED (LF)
				// U+000C FORM FEED (FF)
				// U+0020 SPACE
				// If the current end tag token is an appropriate end tag token, then switch
				// to the before attribute name state. Otherwise, treat it as per the
				// "anything else" entry below.
				if (isSpace(cc)) {
					tagNameEnd = pos;
					if (
						input.slice(tagNameStart, tagNameEnd).toLowerCase() ===
						lastOpenTagName
					) {
						flushText(tagStart);
						state = STATE_BEFORE_ATTRIBUTE_NAME;
						pos++;
					} else {
						state = STATE_RCDATA;
						// Reconsume
					}
				} else if (cc === CC_SOLIDUS) {
					// U+002F SOLIDUS (/)
					// If the current end tag token is an appropriate end tag token, then switch
					// to the self-closing start tag state. Otherwise, treat it as per the
					// "anything else" entry below.
					tagNameEnd = pos;
					if (
						input.slice(tagNameStart, tagNameEnd).toLowerCase() ===
						lastOpenTagName
					) {
						flushText(tagStart);
						state = STATE_SELF_CLOSING_START_TAG;
						pos++;
					} else {
						state = STATE_RCDATA;
						// Reconsume
					}
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// If the current end tag token is an appropriate end tag token, then switch
					// to the data state and emit the current tag token. Otherwise, treat it as
					// per the "anything else" entry below.
					tagNameEnd = pos;
					if (
						input.slice(tagNameStart, tagNameEnd).toLowerCase() ===
						lastOpenTagName
					) {
						flushText(tagStart);
						state = STATE_DATA;
						pos = emitCloseTag(pos + 1);
					} else {
						state = STATE_RCDATA;
						// Reconsume
					}
				} else if (isAsciiAlpha(cc)) {
					// ASCII upper alpha / ASCII lower alpha
					// Append the lowercase version of the current input character to the
					// current tag token's tag name. Append the current input character to
					// the temporary buffer.
					pos++;
				} else {
					// Anything else
					// Emit a U+003C LESS-THAN SIGN character token, a U+002F SOLIDUS character
					// token, and a character token for each of the characters in the temporary
					// buffer (in the order they were added to the buffer). Reconsume in the
					// RCDATA state.
					state = STATE_RCDATA;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#rawtext-state
			case STATE_RAWTEXT:
				// Consume the next input character:
				// U+003C LESS-THAN SIGN (<)
				// Switch to the RAWTEXT less-than sign state.
				if (cc === CC_LESS_THAN) {
					tagStart = pos;
					state = STATE_RAWTEXT_LESS_THAN_SIGN;
					pos++;
				} else {
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#rawtext-less-than-sign-state
			case STATE_RAWTEXT_LESS_THAN_SIGN:
				// Consume the next input character:
				// U+002F SOLIDUS (/)
				// Set the temporary buffer to the empty string. Switch to the RAWTEXT end
				// tag open state.
				if (cc === CC_SOLIDUS) {
					tempBuffer = "";
					state = STATE_RAWTEXT_END_TAG_OPEN;
					pos++;
				} else {
					// Anything else
					// Emit a U+003C LESS-THAN SIGN character token. Reconsume in the RAWTEXT
					// state.
					state = STATE_RAWTEXT;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#rawtext-end-tag-open-state
			case STATE_RAWTEXT_END_TAG_OPEN:
				// Consume the next input character:
				// ASCII alpha
				// Create a new end tag token, set its tag name to the empty string.
				// Reconsume in the RAWTEXT end tag name state.
				if (isAsciiAlpha(cc)) {
					tagNameStart = pos;
					state = STATE_RAWTEXT_END_TAG_NAME;
					// Reconsume
				} else {
					// Anything else
					// Emit a U+003C LESS-THAN SIGN character token and a U+002F SOLIDUS
					// character token. Reconsume in the RAWTEXT state.
					state = STATE_RAWTEXT;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#rawtext-end-tag-name-state
			case STATE_RAWTEXT_END_TAG_NAME:
				// Consume the next input character:
				// U+0009 CHARACTER TABULATION (tab)
				// U+000A LINE FEED (LF)
				// U+000C FORM FEED (FF)
				// U+0020 SPACE
				// If the current end tag token is an appropriate end tag token, then switch
				// to the before attribute name state. Otherwise, treat it as per the
				// "anything else" entry below.
				if (isSpace(cc)) {
					tagNameEnd = pos;
					if (
						input.slice(tagNameStart, tagNameEnd).toLowerCase() ===
						lastOpenTagName
					) {
						flushText(tagStart);
						state = STATE_BEFORE_ATTRIBUTE_NAME;
						pos++;
					} else {
						state = STATE_RAWTEXT;
					}
				} else if (cc === CC_SOLIDUS) {
					// U+002F SOLIDUS (/)
					// If the current end tag token is an appropriate end tag token, then switch
					// to the self-closing start tag state. Otherwise, treat it as per the
					// "anything else" entry below.
					tagNameEnd = pos;
					if (
						input.slice(tagNameStart, tagNameEnd).toLowerCase() ===
						lastOpenTagName
					) {
						flushText(tagStart);
						state = STATE_SELF_CLOSING_START_TAG;
						pos++;
					} else {
						state = STATE_RAWTEXT;
					}
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// If the current end tag token is an appropriate end tag token, then switch
					// to the data state and emit the current tag token. Otherwise, treat it as
					// per the "anything else" entry below.
					tagNameEnd = pos;
					if (
						input.slice(tagNameStart, tagNameEnd).toLowerCase() ===
						lastOpenTagName
					) {
						flushText(tagStart);
						state = STATE_DATA;
						pos = emitCloseTag(pos + 1);
					} else {
						state = STATE_RAWTEXT;
					}
				} else if (isAsciiAlpha(cc)) {
					// ASCII upper alpha / ASCII lower alpha
					// Append the lowercase version of the current input character to the
					// current tag token's tag name. Append the current input character to
					// the temporary buffer.
					pos++;
				} else {
					// Anything else
					// Emit a U+003C LESS-THAN SIGN character token, a U+002F SOLIDUS character
					// token, and a character token for each of the characters in the temporary
					// buffer (in the order they were added to the buffer). Reconsume in the
					// RAWTEXT state.
					state = STATE_RAWTEXT;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#script-data-state
			case STATE_SCRIPT_DATA:
				// Consume the next input character:
				// U+003C LESS-THAN SIGN (<)
				// Switch to the script data less-than sign state.
				if (cc === CC_LESS_THAN) {
					tagStart = pos;
					state = STATE_SCRIPT_DATA_LESS_THAN_SIGN;
					pos++;
				} else {
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#script-data-less-than-sign-state
			case STATE_SCRIPT_DATA_LESS_THAN_SIGN:
				// Consume the next input character:
				// U+002F SOLIDUS (/)
				// Set the temporary buffer to the empty string. Switch to the script data
				// end tag open state.
				if (cc === CC_SOLIDUS) {
					tempBuffer = "";
					state = STATE_SCRIPT_DATA_END_TAG_OPEN;
					pos++;
				} else if (cc === CC_EXCLAMATION_MARK) {
					// U+0021 EXCLAMATION MARK (!)
					// Switch to the script data escape start state. Emit a U+003C LESS-THAN
					// SIGN character token and a U+0021 EXCLAMATION MARK character token.
					state = STATE_SCRIPT_DATA_ESCAPE_START;
					pos++;
				} else {
					// Anything else
					// Emit a U+003C LESS-THAN SIGN character token. Reconsume in the script
					// data state.
					state = STATE_SCRIPT_DATA;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#script-data-end-tag-open-state
			case STATE_SCRIPT_DATA_END_TAG_OPEN:
				// Consume the next input character:
				// ASCII alpha
				// Create a new end tag token, set its tag name to the empty string.
				// Reconsume in the script data end tag name state.
				if (isAsciiAlpha(cc)) {
					tagNameStart = pos;
					state = STATE_SCRIPT_DATA_END_TAG_NAME;
					// Reconsume
				} else {
					// Anything else
					// Emit a U+003C LESS-THAN SIGN character token and a U+002F SOLIDUS
					// character token. Reconsume in the script data state.
					state = STATE_SCRIPT_DATA;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#script-data-end-tag-name-state
			case STATE_SCRIPT_DATA_END_TAG_NAME:
				// Consume the next input character:
				// U+0009 CHARACTER TABULATION (tab)
				// U+000A LINE FEED (LF)
				// U+000C FORM FEED (FF)
				// U+0020 SPACE
				// If the current end tag token is an appropriate end tag token, then switch
				// to the before attribute name state. Otherwise, treat it as per the
				// "anything else" entry below.
				if (isSpace(cc)) {
					tagNameEnd = pos;
					if (
						input.slice(tagNameStart, tagNameEnd).toLowerCase() ===
						lastOpenTagName
					) {
						flushText(tagStart);
						state = STATE_BEFORE_ATTRIBUTE_NAME;
						pos++;
					} else {
						state = STATE_SCRIPT_DATA;
					}
				} else if (cc === CC_SOLIDUS) {
					// U+002F SOLIDUS (/)
					// If the current end tag token is an appropriate end tag token, then switch
					// to the self-closing start tag state. Otherwise, treat it as per the
					// "anything else" entry below.
					tagNameEnd = pos;
					if (
						input.slice(tagNameStart, tagNameEnd).toLowerCase() ===
						lastOpenTagName
					) {
						flushText(tagStart);
						state = STATE_SELF_CLOSING_START_TAG;
						pos++;
					} else {
						state = STATE_SCRIPT_DATA;
					}
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// If the current end tag token is an appropriate end tag token, then switch
					// to the data state and emit the current tag token. Otherwise, treat it as
					// per the "anything else" entry below.
					tagNameEnd = pos;
					if (
						input.slice(tagNameStart, tagNameEnd).toLowerCase() ===
						lastOpenTagName
					) {
						flushText(tagStart);
						state = STATE_DATA;
						pos = emitCloseTag(pos + 1);
					} else {
						state = STATE_SCRIPT_DATA;
					}
				} else if (isAsciiAlpha(cc)) {
					// ASCII upper alpha / ASCII lower alpha
					pos++;
				} else {
					// Anything else
					// Emit a U+003C LESS-THAN SIGN character token, a U+002F SOLIDUS character
					// token, and a character token for each of the characters in the temporary
					// buffer (in the order they were added to the buffer). Reconsume in the
					// script data state.
					state = STATE_SCRIPT_DATA;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#script-data-escape-start-state
			case STATE_SCRIPT_DATA_ESCAPE_START:
				// Consume the next input character:
				// U+002D HYPHEN-MINUS (-)
				// Switch to the script data escape start dash state. Emit a U+002D
				// HYPHEN-MINUS character token.
				if (cc === CC_HYPHEN_MINUS) {
					state = STATE_SCRIPT_DATA_ESCAPE_START_DASH;
					pos++;
				} else {
					// Anything else
					// Reconsume in the script data state.
					state = STATE_SCRIPT_DATA;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#script-data-escape-start-dash-state
			case STATE_SCRIPT_DATA_ESCAPE_START_DASH:
				// Consume the next input character:
				// U+002D HYPHEN-MINUS (-)
				// Switch to the script data escaped dash dash state. Emit a U+002D
				// HYPHEN-MINUS character token.
				if (cc === CC_HYPHEN_MINUS) {
					state = STATE_SCRIPT_DATA_ESCAPED_DASH_DASH;
					pos++;
				} else {
					// Anything else
					// Reconsume in the script data state.
					state = STATE_SCRIPT_DATA;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#script-data-escaped-state
			case STATE_SCRIPT_DATA_ESCAPED:
				// Consume the next input character:
				// U+002D HYPHEN-MINUS (-)
				// Switch to the script data escaped dash state. Emit a U+002D HYPHEN-MINUS
				// character token.
				if (cc === CC_HYPHEN_MINUS) {
					state = STATE_SCRIPT_DATA_ESCAPED_DASH;
					pos++;
				} else if (cc === CC_LESS_THAN) {
					// U+003C LESS-THAN SIGN (<)
					// Switch to the script data escaped less-than sign state.
					tagStart = pos;
					state = STATE_SCRIPT_DATA_ESCAPED_LESS_THAN_SIGN;
					pos++;
				} else {
					// Anything else
					// Emit the current input character as a character token.
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#script-data-escaped-dash-state
			case STATE_SCRIPT_DATA_ESCAPED_DASH:
				// Consume the next input character:
				// U+002D HYPHEN-MINUS (-)
				// Switch to the script data escaped dash dash state. Emit a U+002D
				// HYPHEN-MINUS character token.
				if (cc === CC_HYPHEN_MINUS) {
					state = STATE_SCRIPT_DATA_ESCAPED_DASH_DASH;
					pos++;
				} else if (cc === CC_LESS_THAN) {
					// U+003C LESS-THAN SIGN (<)
					// Switch to the script data escaped less-than sign state.
					tagStart = pos;
					state = STATE_SCRIPT_DATA_ESCAPED_LESS_THAN_SIGN;
					pos++;
				} else {
					// Anything else
					// Switch to the script data escaped state. Emit the current input character
					// as a character token.
					state = STATE_SCRIPT_DATA_ESCAPED;
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#script-data-escaped-dash-dash-state
			case STATE_SCRIPT_DATA_ESCAPED_DASH_DASH:
				// Consume the next input character:
				// U+002D HYPHEN-MINUS (-)
				// Emit a U+002D HYPHEN-MINUS character token.
				if (cc === CC_HYPHEN_MINUS) {
					pos++;
				} else if (cc === CC_LESS_THAN) {
					// U+003C LESS-THAN SIGN (<)
					// Switch to the script data escaped less-than sign state.
					tagStart = pos;
					state = STATE_SCRIPT_DATA_ESCAPED_LESS_THAN_SIGN;
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// Switch to the script data state. Emit a U+003E GREATER-THAN SIGN
					// character token.
					state = STATE_SCRIPT_DATA;
					pos++;
				} else {
					// Anything else
					// Switch to the script data escaped state. Emit the current input character
					// as a character token.
					state = STATE_SCRIPT_DATA_ESCAPED;
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#script-data-escaped-less-than-sign-state
			case STATE_SCRIPT_DATA_ESCAPED_LESS_THAN_SIGN:
				// Consume the next input character:
				// U+002F SOLIDUS (/)
				// Set the temporary buffer to the empty string. Switch to the script data
				// escaped end tag open state.
				if (cc === CC_SOLIDUS) {
					tempBuffer = "";
					state = STATE_SCRIPT_DATA_ESCAPED_END_TAG_OPEN;
					pos++;
				} else if (isAsciiAlpha(cc)) {
					// ASCII alpha
					// Set the temporary buffer to the empty string. Emit a U+003C LESS-THAN
					// SIGN character token. Reconsume in the script data double escape start
					// state.
					tempBuffer = "";
					state = STATE_SCRIPT_DATA_DOUBLE_ESCAPE_START;
					// Reconsume
				} else {
					// Anything else
					// Emit a U+003C LESS-THAN SIGN character token. Reconsume in the script
					// data escaped state.
					state = STATE_SCRIPT_DATA_ESCAPED;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#script-data-escaped-end-tag-open-state
			case STATE_SCRIPT_DATA_ESCAPED_END_TAG_OPEN:
				// Consume the next input character:
				// ASCII alpha
				// Create a new end tag token, set its tag name to the empty string.
				// Reconsume in the script data escaped end tag name state.
				if (isAsciiAlpha(cc)) {
					tagNameStart = pos;
					state = STATE_SCRIPT_DATA_ESCAPED_END_TAG_NAME;
					// Reconsume
				} else {
					// Anything else
					// Emit a U+003C LESS-THAN SIGN character token and a U+002F SOLIDUS
					// character token. Reconsume in the script data escaped state.
					state = STATE_SCRIPT_DATA_ESCAPED;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#script-data-escaped-end-tag-name-state
			case STATE_SCRIPT_DATA_ESCAPED_END_TAG_NAME:
				// Consume the next input character:
				// U+0009 CHARACTER TABULATION (tab)
				// U+000A LINE FEED (LF)
				// U+000C FORM FEED (FF)
				// U+0020 SPACE
				// If the current end tag token is an appropriate end tag token, then switch
				// to the before attribute name state. Otherwise, treat it as per the
				// "anything else" entry below.
				if (isSpace(cc)) {
					tagNameEnd = pos;
					if (
						input.slice(tagNameStart, tagNameEnd).toLowerCase() ===
						lastOpenTagName
					) {
						flushText(tagStart);
						state = STATE_BEFORE_ATTRIBUTE_NAME;
						pos++;
					} else {
						state = STATE_SCRIPT_DATA_ESCAPED;
					}
				} else if (cc === CC_SOLIDUS) {
					// U+002F SOLIDUS (/)
					// If the current end tag token is an appropriate end tag token, then switch
					// to the self-closing start tag state. Otherwise, treat it as per the
					// "anything else" entry below.
					tagNameEnd = pos;
					if (
						input.slice(tagNameStart, tagNameEnd).toLowerCase() ===
						lastOpenTagName
					) {
						flushText(tagStart);
						state = STATE_SELF_CLOSING_START_TAG;
						pos++;
					} else {
						state = STATE_SCRIPT_DATA_ESCAPED;
					}
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// If the current end tag token is an appropriate end tag token, then switch
					// to the data state and emit the current tag token. Otherwise, treat it as
					// per the "anything else" entry below.
					tagNameEnd = pos;
					if (
						input.slice(tagNameStart, tagNameEnd).toLowerCase() ===
						lastOpenTagName
					) {
						flushText(tagStart);
						state = STATE_DATA;
						pos = emitCloseTag(pos + 1);
					} else {
						state = STATE_SCRIPT_DATA_ESCAPED;
					}
				} else if (isAsciiAlpha(cc)) {
					// ASCII upper alpha / ASCII lower alpha
					pos++;
				} else {
					// Anything else
					// Emit a U+003C LESS-THAN SIGN character token, a U+002F SOLIDUS character
					// token, and a character token for each of the characters in the temporary
					// buffer (in the order they were added to the buffer). Reconsume in the
					// script data escaped state.
					state = STATE_SCRIPT_DATA_ESCAPED;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#script-data-double-escape-start-state
			case STATE_SCRIPT_DATA_DOUBLE_ESCAPE_START:
				// Consume the next input character:
				// U+0009 CHARACTER TABULATION (tab)
				// U+000A LINE FEED (LF)
				// U+000C FORM FEED (FF)
				// U+0020 SPACE
				// U+002F SOLIDUS (/)
				// U+003E GREATER-THAN SIGN (>)
				// If the temporary buffer is the string "script", then switch to the script
				// data double escaped state. Otherwise, switch to the script data escaped
				// state. Emit the current input character as a character token.
				if (isSpace(cc) || cc === CC_SOLIDUS || cc === CC_GREATER_THAN) {
					state =
						tempBuffer === "script"
							? STATE_SCRIPT_DATA_DOUBLE_ESCAPED
							: STATE_SCRIPT_DATA_ESCAPED;
					pos++;
				} else if (isAsciiUpperAlpha(cc)) {
					// ASCII upper alpha
					// Append the lowercase version of the current input character (add 0x0020
					// to the character's code point) to the temporary buffer. Emit the current
					// input character as a character token.
					tempBuffer += String.fromCharCode(cc + 0x20);
					pos++;
				} else if (isAsciiLowerAlpha(cc)) {
					// ASCII lower alpha
					// Append the current input character to the temporary buffer. Emit the
					// current input character as a character token.
					tempBuffer += String.fromCharCode(cc);
					pos++;
				} else {
					// Anything else
					// Reconsume in the script data escaped state.
					state = STATE_SCRIPT_DATA_ESCAPED;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#script-data-double-escaped-state
			case STATE_SCRIPT_DATA_DOUBLE_ESCAPED:
				// Consume the next input character:
				// U+002D HYPHEN-MINUS (-)
				// Switch to the script data double escaped dash state. Emit a U+002D
				// HYPHEN-MINUS character token.
				if (cc === CC_HYPHEN_MINUS) {
					state = STATE_SCRIPT_DATA_DOUBLE_ESCAPED_DASH;
					pos++;
				} else if (cc === CC_LESS_THAN) {
					// U+003C LESS-THAN SIGN (<)
					// Switch to the script data double escaped less-than sign state. Emit a
					// U+003C LESS-THAN SIGN character token.
					state = STATE_SCRIPT_DATA_DOUBLE_ESCAPED_LESS_THAN_SIGN;
					pos++;
				} else {
					// Anything else
					// Emit the current input character as a character token.
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#script-data-double-escaped-dash-state
			case STATE_SCRIPT_DATA_DOUBLE_ESCAPED_DASH:
				// Consume the next input character:
				// U+002D HYPHEN-MINUS (-)
				// Switch to the script data double escaped dash dash state. Emit a U+002D
				// HYPHEN-MINUS character token.
				if (cc === CC_HYPHEN_MINUS) {
					state = STATE_SCRIPT_DATA_DOUBLE_ESCAPED_DASH_DASH;
					pos++;
				} else if (cc === CC_LESS_THAN) {
					// U+003C LESS-THAN SIGN (<)
					// Switch to the script data double escaped less-than sign state. Emit a
					// U+003C LESS-THAN SIGN character token.
					state = STATE_SCRIPT_DATA_DOUBLE_ESCAPED_LESS_THAN_SIGN;
					pos++;
				} else {
					// Anything else
					// Switch to the script data double escaped state. Emit the current input
					// character as a character token.
					state = STATE_SCRIPT_DATA_DOUBLE_ESCAPED;
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#script-data-double-escaped-dash-dash-state
			case STATE_SCRIPT_DATA_DOUBLE_ESCAPED_DASH_DASH:
				// Consume the next input character:
				// U+002D HYPHEN-MINUS (-)
				// Emit a U+002D HYPHEN-MINUS character token.
				if (cc === CC_HYPHEN_MINUS) {
					pos++;
				} else if (cc === CC_LESS_THAN) {
					// U+003C LESS-THAN SIGN (<)
					// Switch to the script data double escaped less-than sign state. Emit a
					// U+003C LESS-THAN SIGN character token.
					state = STATE_SCRIPT_DATA_DOUBLE_ESCAPED_LESS_THAN_SIGN;
					pos++;
				} else if (cc === CC_GREATER_THAN) {
					// U+003E GREATER-THAN SIGN (>)
					// Switch to the script data state. Emit a U+003E GREATER-THAN SIGN
					// character token.
					state = STATE_SCRIPT_DATA;
					pos++;
				} else {
					// Anything else
					// Switch to the script data double escaped state. Emit the current input
					// character as a character token.
					state = STATE_SCRIPT_DATA_DOUBLE_ESCAPED;
					pos++;
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#script-data-double-escaped-less-than-sign-state
			case STATE_SCRIPT_DATA_DOUBLE_ESCAPED_LESS_THAN_SIGN:
				// Consume the next input character:
				// U+002F SOLIDUS (/)
				// Set the temporary buffer to the empty string. Switch to the script data
				// double escape end state. Emit a U+002F SOLIDUS character token.
				if (cc === CC_SOLIDUS) {
					tempBuffer = "";
					state = STATE_SCRIPT_DATA_DOUBLE_ESCAPE_END;
					pos++;
				} else {
					// Anything else
					// Reconsume in the script data double escaped state.
					state = STATE_SCRIPT_DATA_DOUBLE_ESCAPED;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#script-data-double-escape-end-state
			case STATE_SCRIPT_DATA_DOUBLE_ESCAPE_END:
				// Consume the next input character:
				// U+0009 CHARACTER TABULATION (tab)
				// U+000A LINE FEED (LF)
				// U+000C FORM FEED (FF)
				// U+0020 SPACE
				// U+002F SOLIDUS (/)
				// U+003E GREATER-THAN SIGN (>)
				// If the temporary buffer is the string "script", then switch to the script
				// data escaped state. Otherwise, switch to the script data double escaped
				// state. Emit the current input character as a character token.
				if (isSpace(cc) || cc === CC_SOLIDUS || cc === CC_GREATER_THAN) {
					state =
						tempBuffer === "script"
							? STATE_SCRIPT_DATA_ESCAPED
							: STATE_SCRIPT_DATA_DOUBLE_ESCAPED;
					pos++;
				} else if (isAsciiUpperAlpha(cc)) {
					// ASCII upper alpha
					// Append the lowercase version of the current input character (add 0x0020
					// to the character's code point) to the temporary buffer. Emit the current
					// input character as a character token.
					tempBuffer += String.fromCharCode(cc + 0x20);
					pos++;
				} else if (isAsciiLowerAlpha(cc)) {
					// ASCII lower alpha
					// Append the current input character to the temporary buffer. Emit the
					// current input character as a character token.
					tempBuffer += String.fromCharCode(cc);
					pos++;
				} else {
					// Anything else
					// Reconsume in the script data double escaped state.
					state = STATE_SCRIPT_DATA_DOUBLE_ESCAPED;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#plaintext-state
			case STATE_PLAINTEXT:
				// Consume the next input character:
				// Anything else
				// Emit the current input character as a character token.
				pos++;
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#character-reference-state
			case STATE_CHARACTER_REFERENCE:
				// Set the temporary buffer to the empty string. Append a U+0026
				// AMPERSAND (&) character to the temporary buffer.
				// Consume the next input character:
				if (isAsciiAlphanumeric(cc)) {
					// ASCII alphanumeric
					// Reconsume in the named character reference state.
					state = STATE_NAMED_CHARACTER_REFERENCE;
					// Reconsume
				} else if (cc === CC_NUMBER_SIGN) {
					// U+0023 NUMBER SIGN (#)
					// Append the current input character to the temporary buffer.
					// Switch to the numeric character reference state.
					state = STATE_NUMERIC_CHARACTER_REFERENCE;
					pos++;
				} else {
					// Anything else
					// Flush code points consumed as a character reference.
					// Reconsume in the return state.
					state = returnState;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#named-character-reference-state
			case STATE_NAMED_CHARACTER_REFERENCE: {
				// Consume the maximum number of characters possible where the
				// consumed characters are one of the identifiers in the first
				// column of the named character references table.
				//
				// We measure the longest run of ASCII alphanumeric characters
				// (capped at 32, the longest entity name without the leading
				// `&`), then walk that run from longest to shortest looking for
				// the first prefix that exists in the entity table (with a
				// trailing `;` if present, otherwise the legacy bare form).
				let runLen = 0;
				while (
					pos + runLen < len &&
					isAsciiAlphanumeric(input.charCodeAt(pos + runLen))
				) {
					runLen++;
					// Safety cap — the longest entity name is 32 chars (without `&`).
					if (runLen > 32) break;
				}
				const hasSemicolon =
					pos + runLen < len && input.charCodeAt(pos + runLen) === CC_SEMICOLON;
				namedEntityConsumed = 0;
				const entities = getHtmlEntities();
				for (let n = runLen; n > 0; n--) {
					// Try with trailing `;` first if one is present after the run.
					if (n === runLen && hasSemicolon) {
						const withSemi = `${input.slice(pos, pos + n)};`;
						if (entities[withSemi] !== undefined) {
							namedEntityConsumed = n + 1;
							break;
						}
					}
					const bare = input.slice(pos, pos + n);
					if (entities[bare] !== undefined) {
						namedEntityConsumed = n;
						break;
					}
				}
				if (namedEntityConsumed > 0) {
					pos += namedEntityConsumed;
					state = returnState;
				} else {
					// No match — flush code points consumed as a character
					// reference. Switch to the ambiguous ampersand state.
					state = STATE_AMBIGUOUS_AMPERSAND;
				}
				break;
			}

			// https://html.spec.whatwg.org/multipage/parsing.html#ambiguous-ampersand-state
			case STATE_AMBIGUOUS_AMPERSAND:
				// Consume the next input character:
				if (isAsciiAlphanumeric(cc)) {
					// ASCII alphanumeric
					// If the character reference was consumed as part of an
					// attribute, then append the current input character to the
					// current attribute's value. Otherwise, emit the current
					// input character as a character token.
					pos++;
				} else if (cc === CC_SEMICOLON) {
					// U+003B SEMICOLON (;)
					// This is an unknown-named-character-reference parse error.
					// Reconsume in the return state.
					reportError(
						"unknown-named-character-reference",
						pos,
						pos + 1,
						"warning"
					);
					state = returnState;
					// Reconsume
				} else {
					// Anything else
					// Reconsume in the return state.
					state = returnState;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#numeric-character-reference-state
			case STATE_NUMERIC_CHARACTER_REFERENCE:
				// Set the character reference code to zero (0).
				// Consume the next input character:
				if (cc === 0x78 || cc === 0x58) {
					// U+0078 LATIN SMALL LETTER X
					// U+0058 LATIN CAPITAL LETTER X
					// Append the current input character to the temporary
					// buffer. Switch to the hexadecimal character reference
					// start state.
					state = STATE_HEXADECIMAL_CHARACTER_REFERENCE_START;
					pos++;
				} else {
					// Anything else
					// Reconsume in the decimal character reference start state.
					state = STATE_DECIMAL_CHARACTER_REFERENCE_START;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#hexadecimal-character-reference-start-state
			case STATE_HEXADECIMAL_CHARACTER_REFERENCE_START:
				// Consume the next input character:
				// ASCII hex digit: reconsume in the hexadecimal character reference state.
				// Anything else: absence-of-digits-in-numeric-character-reference parse
				// error. Flush code points consumed as a character reference. Reconsume
				// in the return state.
				if (isAsciiHexDigit(cc)) {
					state = STATE_HEXADECIMAL_CHARACTER_REFERENCE;
				} else {
					reportError(
						"absence-of-digits-in-numeric-character-reference",
						pos,
						pos + 1,
						"warning"
					);
					state = returnState;
				}
				// Reconsume
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#decimal-character-reference-start-state
			case STATE_DECIMAL_CHARACTER_REFERENCE_START:
				// Consume the next input character:
				// ASCII digit: reconsume in the decimal character reference state.
				// Anything else: absence-of-digits-in-numeric-character-reference parse
				// error. Flush code points consumed as a character reference. Reconsume
				// in the return state.
				if (isAsciiDigit(cc)) {
					state = STATE_DECIMAL_CHARACTER_REFERENCE;
				} else {
					reportError(
						"absence-of-digits-in-numeric-character-reference",
						pos,
						pos + 1,
						"warning"
					);
					state = returnState;
				}
				// Reconsume
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#hexadecimal-character-reference-state
			case STATE_HEXADECIMAL_CHARACTER_REFERENCE:
				// Consume the next input character:
				if (isAsciiHexDigit(cc)) {
					// ASCII digit / upper hex / lower hex
					// Multiply the character reference code by 16. Add a numeric
					// version of the current input character to the character
					// reference code.
					pos++;
				} else if (cc === CC_SEMICOLON) {
					// U+003B SEMICOLON
					// Switch to the numeric character reference end state.
					state = STATE_NUMERIC_CHARACTER_REFERENCE_END;
					pos++;
				} else {
					// Anything else
					// This is a missing-semicolon-after-character-reference
					// parse error. Reconsume in the numeric character reference
					// end state.
					reportError(
						"missing-semicolon-after-character-reference",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_NUMERIC_CHARACTER_REFERENCE_END;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#decimal-character-reference-state
			case STATE_DECIMAL_CHARACTER_REFERENCE:
				// Consume the next input character:
				if (isAsciiDigit(cc)) {
					// ASCII digit
					// Multiply the character reference code by 10. Add a numeric
					// version of the current input character (subtract 0x0030
					// from the character's code point) to the character reference
					// code.
					pos++;
				} else if (cc === CC_SEMICOLON) {
					// U+003B SEMICOLON
					// Switch to the numeric character reference end state.
					state = STATE_NUMERIC_CHARACTER_REFERENCE_END;
					pos++;
				} else {
					// Anything else
					// This is a missing-semicolon-after-character-reference
					// parse error. Reconsume in the numeric character reference
					// end state.
					reportError(
						"missing-semicolon-after-character-reference",
						pos,
						pos + 1,
						"warning"
					);
					state = STATE_NUMERIC_CHARACTER_REFERENCE_END;
					// Reconsume
				}
				break;

			// https://html.spec.whatwg.org/multipage/parsing.html#numeric-character-reference-end-state
			case STATE_NUMERIC_CHARACTER_REFERENCE_END:
				// Check the character reference code (validation omitted for
				// the scanner — we don't decode, just skip past the entity).
				// Flush code points consumed as a character reference.
				// Switch to the return state.
				state = returnState;
				// Reconsume
				break;

			/* istanbul ignore next -- @preserve: defensive fallback, all states are explicit above */
			default:
				pos++;
		}
	}

	// Handle EOF in non-data states per the WHATWG spec.
	//
	// Each in-progress comment / doctype / cdata / tag emits its partial
	// token range plus a corresponding `eof-in-X` parse error. Severity is
	// `"error"` because the emitted token byte range is incomplete (missing
	// trailing `-->`, `>`, `]]>`, etc.). For data / `<` / `</` / `<!`-only
	// inputs we emit `eof-before-tag-name` and fall through to flush the
	// pending text span (which still contains the lone `<`).
	if (
		(state >= STATE_TAG_NAME && state <= STATE_SELF_CLOSING_START_TAG) ||
		state === STATE_RCDATA_END_TAG_NAME ||
		state === STATE_RAWTEXT_END_TAG_NAME ||
		state === STATE_SCRIPT_DATA_END_TAG_NAME ||
		state === STATE_SCRIPT_DATA_ESCAPED_END_TAG_NAME
	) {
		// EOF mid-tag — emit the partial open/close tag at EOF so the
		// consumer still sees the tag. This is a deliberate deviation
		// from the byte-stream-only view of the spec: rather than dropping
		// the in-progress tag, we emit its byte range up to EOF.
		reportError("eof-in-tag", len, len, "error");
		if (attrNameStart !== -1) emitAttribute(len);
		// If we hit EOF before the tag-name end was recorded (e.g.
		// `<div</a`), the name runs to EOF. We need to update tagNameEnd
		// regardless of its previous value (it may carry over from a
		// completed prior tag).
		if (state === STATE_TAG_NAME) tagNameEnd = len;
		flushText(tagStart);
		pos =
			input.charCodeAt(tagStart + 1) === CC_SOLIDUS
				? emitCloseTag(len)
				: emitOpenTag(len, false);
	} else if (
		(state >= STATE_COMMENT_START && state <= STATE_BOGUS_COMMENT) ||
		(state >= STATE_COMMENT_LESS_THAN_SIGN &&
			state <= STATE_COMMENT_LESS_THAN_SIGN_BANG_DASH_DASH) ||
		state === STATE_MARKUP_DECLARATION_OPEN
	) {
		reportError("eof-in-comment", len, len, "error");
		if (callbacks.comment !== undefined) {
			pos = callbacks.comment(input, commentStart, len);
		}
	} else if (state >= STATE_CDATA_SECTION && state <= STATE_CDATA_SECTION_END) {
		reportError("eof-in-cdata", len, len, "error");
		if (callbacks.comment !== undefined) {
			pos = callbacks.comment(input, commentStart, len);
		}
	} else if (state >= STATE_DOCTYPE && state <= STATE_BOGUS_DOCTYPE) {
		reportError("eof-in-doctype", len, len, "error");
		if (callbacks.doctype !== undefined) {
			pos = callbacks.doctype(input, commentStart, len);
		}
	} else {
		if (
			state === STATE_SCRIPT_DATA_ESCAPED ||
			state === STATE_SCRIPT_DATA_ESCAPED_DASH ||
			state === STATE_SCRIPT_DATA_ESCAPED_DASH_DASH ||
			state === STATE_SCRIPT_DATA_DOUBLE_ESCAPED ||
			state === STATE_SCRIPT_DATA_DOUBLE_ESCAPED_DASH ||
			state === STATE_SCRIPT_DATA_DOUBLE_ESCAPED_DASH_DASH
		) {
			// Inside `<script><!-- … ` at EOF — spec calls this an
			// eof-in-script-html-comment-like-text parse error.
			reportError("eof-in-script-html-comment-like-text", len, len, "error");
		} else if (state === STATE_TAG_OPEN || state === STATE_END_TAG_OPEN) {
			// `<` or `</` with nothing after; spec calls this
			// eof-before-tag-name. The lone `<` / `</` is preserved in the
			// pending text span which is flushed below.
			reportError("eof-before-tag-name", len, len, "warning");
		}
		if (textStart < len && callbacks.text !== undefined) {
			callbacks.text(input, textStart, len);
		}
	}

	return pos;
};

walkHtmlTokens.QUOTE_NONE = QUOTE_NONE;
walkHtmlTokens.QUOTE_SINGLE = QUOTE_SINGLE;
walkHtmlTokens.QUOTE_DOUBLE = QUOTE_DOUBLE;

/**
 * Decode HTML character references in a string. Handles all numeric
 * references and the full WHATWG named character references table. Unknown
 * or malformed references are left as literal text.
 * @param {string} str the raw string from the token slice
 * @returns {string} decoded string
 */
walkHtmlTokens.decodeHtmlEntities = (str) => {
	if (!str.includes("&")) return str;

	let entities;

	// Match either `&name;` / `&name` (named refs may be legacy-bare per the
	// WHATWG entities table) or a numeric reference `&#\u2026;?`.
	return str.replace(/&(#[xX]?[0-9a-fA-F]+|#?[0-9a-zA-Z]+);?/g, (match) => {
		// Numeric reference: &#65; or &#x41;
		if (match.charCodeAt(1) === 0x23 /* # */) {
			const lastChar = match.charAt(match.length - 1);
			const body = lastChar === ";" ? match.slice(2, -1) : match.slice(2);
			const isHex = body.charCodeAt(0) === 0x78 || body.charCodeAt(0) === 0x58;
			const code = isHex
				? Number.parseInt(body.slice(1), 16)
				: Number.parseInt(body, 10);
			if (!Number.isNaN(code)) {
				// Out-of-Unicode-range \u2192 replacement character per WHATWG.
				return code > 0x10ffff ? "\uFFFD" : String.fromCodePoint(code);
			}
			return match; // Invalid numeric (e.g. &#;)
		}

		// Named reference. Try the full captured name first, then progressively
		// shorter prefixes \u2014 this handles direct matches like `&amp;` as well
		// as WHATWG longest-prefix semantics where e.g. `&notpre;` decodes as
		// `&not` (a legacy bare entity) followed by `pre;` as literal text.
		entities = entities || getHtmlEntities();
		const name = match.slice(1);
		for (let i = name.length; i > 0; i--) {
			const prefix = name.slice(0, i);
			if (entities[prefix] !== undefined) {
				return entities[prefix] + name.slice(i);
			}
		}
		return match;
	});
};

module.exports = walkHtmlTokens;
