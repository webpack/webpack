// This example uses `webpack.config.js` (a MultiCompiler config); each entry
// there is a self-contained scenario. Point your browser at any of the
// generated `dist/<name>/*.html` files to see the emitted `<link>` tags.
console.log("See src/routes/*.js and webpack.config.js");
