{
  "definitions": {},
  "title": "SSRManifestPluginOptions",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "context": {
      "description": "The base directory used to compute the source-module keys (defaults to the compiler context).",
      "type": "string",
      "absolutePath": true,
      "added": "5.111.0"
    },
    "filename": {
      "description": "Specifies the filename of the emitted manifest on disk. By default the plugin will emit `ssr-manifest.json` inside the 'output.path' directory.",
      "type": "string",
      "absolutePath": false,
      "minLength": 1,
      "added": "5.111.0"
    }
  }
}
