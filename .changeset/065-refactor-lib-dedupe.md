---
"webpack": patch
---

Reduce duplication and name magic constants across several lib modules (resource-hint asset walk, runtime-module and stats-printer tap registration, ModuleConcatenation failure caching, FileSystemInfo snapshot flags) with no behavior change.
