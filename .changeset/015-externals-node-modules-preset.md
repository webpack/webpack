---
"webpack": minor
---

Add the `externalsPresets.nodeModules` preset to externalize installed packages, with an `allowlist` option to keep specific requests bundled; use it instead of the `webpack-node-externals` plugin.
