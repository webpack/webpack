---
"webpack": minor
---

Name each CSS minifier rewrite on `optimization.minimize.css` — `colors`, `escapes`, `functions`, `lowercase`, `mediaQueries`, `numbers`, `quotes`, `rules`, `selectors`, `shorthands` — so one a document trips over can be turned off while the rest still apply.
