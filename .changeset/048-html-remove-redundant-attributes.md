---
"webpack": minor
---

Fold adjacent `<style>` into one and drop repeated `class` tokens, and add `optimization.minimize.html` options `removeEmptyAttributes`, `removeEmptyElements`, `sortAttributes`, `sortClassNames` and `removeRedundantAttributes: "all"`.
