---
"webpack": minor
---

Fold adjacent `<style>` into one and drop repeated `class` tokens, and add `optimization.minimize.html` options `removeEmptyAttributes`, `sortAttributes`, `sortClassNames` and `removeRedundantAttributes: "all"`.
