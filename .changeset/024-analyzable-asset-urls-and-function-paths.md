---
"webpack": minor
---

Bake analyzable asset references per entry `baseUri`, settle them into the javascript wrapper a default import reads, and keep chunks whose names could go stale out of the deferred fill.
