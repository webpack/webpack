---
"webpack": minor
---

Name each HTML minifier rewrite on `optimization.minimize.html` — `booleanAttributes`, `comments`, `enumeratedAttributes`, `listAttributes`, `minifyJson`, `minifyStyles`, `numericAttributes`, `optionalTags`, `quotes`, `urlAttributes` — so one a page trips over can be turned off while the rest still apply.
