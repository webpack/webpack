---
"webpack": minor
---

Add `renderEmbeddedSource` to transform source one language embeds in another, minify it out of the box through `cssMinify` / `htmlMinify` — an HTML `style=""` included, as the declaration list it is — and reach it in the same parse that prints.
