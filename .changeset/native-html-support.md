---
"webpack": minor
---

Implement native HTML module support as an experiment. This allows using HTML files as entry points and automatically discovering dependencies in `<script>`, `<link>`, and `<img>` tags.
