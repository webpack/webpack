---
"webpack": patch
---

Keep a container style query and `@function`'s `result` as written, and build the tree an engine builds for a declarative shadow root and for a formatting end tag the active list no longer names.
