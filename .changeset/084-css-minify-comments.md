---
"webpack": minor
---

Name the CSS minimizer's comment handling on `optimization.minimize.css`: `comments` (`true` keeps the banners, `false` keeps everything, `"all"` keeps nothing) and `preserveComments` patterns, and keep the CSS and HTML minimizers' same-named options apart.
