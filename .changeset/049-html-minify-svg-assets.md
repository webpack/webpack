---
"webpack": minor
---

Minify SVG: a standalone `.svg` asset printed as XML with an empty element spelled `<name/>`, an svg `path`'s `d`, a presentation attribute as the CSS value it is, and the payload of a `data:image/svg+xml` URL in CSS.
