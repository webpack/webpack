---
"webpack": minor
---

Add and drop CSS vendor prefixes when minifying for a browserslist target — properties, at-rules and pseudo-selectors carry exactly the `-webkit-` / `-moz-` / `-ms-` spellings the selected browsers still need.
