---
"webpack": minor
---

Add `output.environment.cssColorHexAlpha`, resolved from the target like the JavaScript abilities beside it, so CSS minification only shortens a fully transparent color to `#0000` where the target reads a hex alpha.
