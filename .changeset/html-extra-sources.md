---
"webpack": minor
---

Recognize more asset-bearing HTML sources: the `twitter:player:stream` meta content and the legacy SVG `font-face-uri`, `cursor`, `altGlyph`, `tref`, and `glyphRef` element references.
