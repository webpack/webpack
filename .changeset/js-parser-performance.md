---
"webpack": patch
---

Speed up JavaScript parsing with lazy AST locations, Set-based scopes, V8-backed RegExp validation, cached evaluate snippets, type-keyed statement hooks and built-in import phases parsing.
