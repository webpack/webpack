---
"webpack": minor
---

Safely minify CSS (with source maps) and HTML assets when `optimization.minimize` is enabled, unless a minimizer is already configured for them; CSS also shortens colors, numbers, easing functions and quoting and drops empty rules, and HTML collapses opening-tag whitespace.
