---
"webpack": patch
---

Add configuration validation for VirtualUrlPlugin to warn when reserved properties (type) are incorrectly placed at the top level instead of inside virtual module configuration objects.
