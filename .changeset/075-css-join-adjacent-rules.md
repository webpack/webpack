---
"webpack": minor
---

Join adjacent CSS rules that print the same block into one selector list and adjacent at-rule blocks of one condition into one block, at every nesting depth, and keep a declaration a nested rule parts from its repeat.
