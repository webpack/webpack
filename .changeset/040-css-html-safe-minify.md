---
"webpack": patch
---

Only make CSS and HTML transformations an engine cannot tell apart: keep as written every value, selector and attribute a rewrite would change the meaning of, read a lone `{}` block as a declaration's whole value, and build the tree an engine builds for a declarative shadow root and for a formatting end tag the active list no longer names.
