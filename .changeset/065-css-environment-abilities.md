---
"webpack": minor
---

Add `output.environment.cssColorHexAlpha`, `cssInsetShorthand` and `cssMediaQueryRange`, resolved from the target like the JavaScript abilities beside them, so CSS minification only reaches for a spelling the target can read.
