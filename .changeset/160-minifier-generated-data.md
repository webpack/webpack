---
"webpack": patch
---

Generate every CSS and HTML lookup table into `lib/{css,html}/data.js`, the HTML ones distilled from the spec's IDL and the CSS ones now including the value-type facts read out of the value-definition grammars — a range `mdn-data` writes outside the type it bounds among them — leaving both `syntax.js` files algorithm only.
