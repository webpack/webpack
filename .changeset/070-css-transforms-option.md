---
"webpack": minor
---

Keep a `@supports` condition's value as written, since it is the declaration being tested, and add a `transforms.customPropertyValues` minimizer option for rewriting a custom property's value.
