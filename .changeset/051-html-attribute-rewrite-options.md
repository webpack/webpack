---
"webpack": minor
---

Add `collapseBooleanAttributes`, `normalizeAttributeValues`, `minifyStyleAttribute` and `removeAttributeQuotes` to `optimization.minimize.html`, so an attribute rewrite a consumer reads the bytes of can be turned off.
