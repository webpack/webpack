---
"webpack": patch
---

Collapse the boolean HTML attributes the spec states that were missing: `alpha`, `img`'s `controls`, the `shadowroot*` trio, and the obsolete `compact`, `declare`, `nohref`, `noshade` and `nowrap`.
