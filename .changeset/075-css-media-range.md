---
"webpack": minor
---

Add `output.environment.cssMediaQueryRange`, resolved from the target, so CSS minification writes a media feature as `(width>=600px)` where the target reads the range syntax.
