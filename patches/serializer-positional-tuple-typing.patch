diff --git a/lib/dependencies/ConstDependency.js b/lib/dependencies/ConstDependency.js
index 97ae046..cea8114 100644
--- a/lib/dependencies/ConstDependency.js
+++ b/lib/dependencies/ConstDependency.js
@@ -16,8 +16,8 @@ const NullDependency = require("./NullDependency");
 /** @typedef {import("../ModuleGraph")} ModuleGraph */
 /** @typedef {import("../ModuleGraphConnection").ConnectionState} ConnectionState */
 /** @typedef {import("../javascript/JavascriptParser").Range} Range */
-/** @typedef {import("../serialization/ObjectMiddleware").ObjectDeserializerContext} ObjectDeserializerContext */
-/** @typedef {import("../serialization/ObjectMiddleware").ObjectSerializerContext} ObjectSerializerContext */
+/** @typedef {import("../serialization/ObjectMiddleware").ObjectDeserializerContext<[string, number | Range, Set<string> | null]>} ObjectDeserializerContext */
+/** @typedef {import("../serialization/ObjectMiddleware").ObjectSerializerContext<[string, number | Range, Set<string> | null]>} ObjectSerializerContext */
 /** @typedef {import("../util/Hash")} Hash */
 
 class ConstDependency extends NullDependency {
@@ -72,10 +72,11 @@ class ConstDependency extends NullDependency {
 	 * @param {ObjectSerializerContext} context context
 	 */
 	serialize(context) {
-		const { write } = context;
-		write(this.expression);
-		write(this.range);
-		write(this.runtimeRequirements);
+		// chain writes so each position is type-checked against the tuple
+		context
+			.write(this.expression)
+			.write(this.range)
+			.write(this.runtimeRequirements);
 		super.serialize(context);
 	}
 
@@ -84,11 +85,13 @@ class ConstDependency extends NullDependency {
 	 * @param {ObjectDeserializerContext} context context
 	 */
 	deserialize(context) {
-		const { read } = context;
-		this.expression = read();
-		this.range = read();
-		this.runtimeRequirements = read();
-		super.deserialize(context);
+		// advance the cursor via `rest` so each read is type-checked by position
+		this.expression = context.read();
+		const c1 = context.rest;
+		this.range = c1.read();
+		const c2 = c1.rest;
+		this.runtimeRequirements = c2.read();
+		super.deserialize(c2.rest);
 	}
 }
 
diff --git a/lib/serialization/AggregateErrorSerializer.js b/lib/serialization/AggregateErrorSerializer.js
index 2daf0a3..974b4de 100644
--- a/lib/serialization/AggregateErrorSerializer.js
+++ b/lib/serialization/AggregateErrorSerializer.js
@@ -5,8 +5,8 @@
 "use strict";
 
 /** @typedef {import("./types").ComplexSerializableType} ComplexSerializableType */
-/** @typedef {import("./ObjectMiddleware").ObjectDeserializerContext<ComplexSerializableType>} ObjectDeserializerContext */
-/** @typedef {import("./ObjectMiddleware").ObjectSerializerContext<ComplexSerializableType>} ObjectSerializerContext */
+/** @typedef {import("./ObjectMiddleware").ObjectDeserializerContext<ComplexSerializableType[]>} ObjectDeserializerContext */
+/** @typedef {import("./ObjectMiddleware").ObjectSerializerContext<ComplexSerializableType[]>} ObjectSerializerContext */
 
 /** @typedef {Error & { cause?: unknown, errors: EXPECTED_ANY[] }} AggregateError */
 
diff --git a/lib/serialization/ArraySerializer.js b/lib/serialization/ArraySerializer.js
index 9818371..ab09ebf 100644
--- a/lib/serialization/ArraySerializer.js
+++ b/lib/serialization/ArraySerializer.js
@@ -9,7 +9,7 @@ class ArraySerializer {
 	 * Serializes this instance into the provided serializer context.
 	 * @template T
 	 * @param {T[]} array array
-	 * @param {import("./ObjectMiddleware").ObjectSerializerContext<number | T>} context context
+	 * @param {import("./ObjectMiddleware").ObjectSerializerContext<Array<number | T>>} context context
 	 */
 	serialize(array, context) {
 		context.write(array.length);
@@ -19,7 +19,7 @@ class ArraySerializer {
 	/**
 	 * Restores this instance from the provided deserializer context.
 	 * @template T
-	 * @param {import("./ObjectMiddleware").ObjectDeserializerContext<number | T>} context context
+	 * @param {import("./ObjectMiddleware").ObjectDeserializerContext<Array<number | T>>} context context
 	 * @returns {T[]} array
 	 */
 	deserialize(context) {
diff --git a/lib/serialization/DateObjectSerializer.js b/lib/serialization/DateObjectSerializer.js
index 3832b35..48e85af 100644
--- a/lib/serialization/DateObjectSerializer.js
+++ b/lib/serialization/DateObjectSerializer.js
@@ -4,8 +4,8 @@
 
 "use strict";
 
-/** @typedef {import("./ObjectMiddleware").ObjectDeserializerContext<number>} ObjectDeserializerContext */
-/** @typedef {import("./ObjectMiddleware").ObjectSerializerContext<number>} ObjectSerializerContext */
+/** @typedef {import("./ObjectMiddleware").ObjectDeserializerContext<[number]>} ObjectDeserializerContext */
+/** @typedef {import("./ObjectMiddleware").ObjectSerializerContext<[number]>} ObjectSerializerContext */
 
 class DateObjectSerializer {
 	/**
diff --git a/lib/serialization/ErrorObjectSerializer.js b/lib/serialization/ErrorObjectSerializer.js
index f719015..eb82ecf 100644
--- a/lib/serialization/ErrorObjectSerializer.js
+++ b/lib/serialization/ErrorObjectSerializer.js
@@ -5,8 +5,8 @@
 "use strict";
 
 /** @typedef {import("./types").ComplexSerializableType} ComplexSerializableType */
-/** @typedef {import("./ObjectMiddleware").ObjectDeserializerContext<ComplexSerializableType>} ObjectDeserializerContext */
-/** @typedef {import("./ObjectMiddleware").ObjectSerializerContext<ComplexSerializableType>} ObjectSerializerContext */
+/** @typedef {import("./ObjectMiddleware").ObjectDeserializerContext<ComplexSerializableType[]>} ObjectDeserializerContext */
+/** @typedef {import("./ObjectMiddleware").ObjectSerializerContext<ComplexSerializableType[]>} ObjectSerializerContext */
 
 /** @typedef {Error & { cause?: unknown }} ErrorWithCause */
 
diff --git a/lib/serialization/MapObjectSerializer.js b/lib/serialization/MapObjectSerializer.js
index 63592ab..c85a6b7 100644
--- a/lib/serialization/MapObjectSerializer.js
+++ b/lib/serialization/MapObjectSerializer.js
@@ -9,7 +9,7 @@ class MapObjectSerializer {
 	 * Serializes this instance into the provided serializer context.
 	 * @template K, V
 	 * @param {Map<K, V>} obj map
-	 * @param {import("./ObjectMiddleware").ObjectSerializerContext<number | K | V>} context context
+	 * @param {import("./ObjectMiddleware").ObjectSerializerContext<Array<number | K | V>>} context context
 	 */
 	serialize(obj, context) {
 		context.write(obj.size);
@@ -24,7 +24,7 @@ class MapObjectSerializer {
 	/**
 	 * Restores this instance from the provided deserializer context.
 	 * @template K, V
-	 * @param {import("./ObjectMiddleware").ObjectDeserializerContext<number | K | V>} context context
+	 * @param {import("./ObjectMiddleware").ObjectDeserializerContext<Array<number | K | V>>} context context
 	 * @returns {Map<K, V>} map
 	 */
 	deserialize(context) {
diff --git a/lib/serialization/NullPrototypeObjectSerializer.js b/lib/serialization/NullPrototypeObjectSerializer.js
index a887ada..17c669b 100644
--- a/lib/serialization/NullPrototypeObjectSerializer.js
+++ b/lib/serialization/NullPrototypeObjectSerializer.js
@@ -11,7 +11,7 @@ class NullPrototypeObjectSerializer {
 	 * Serializes this instance into the provided serializer context.
 	 * @template {object} T
 	 * @param {T} obj null object
-	 * @param {import("./ObjectMiddleware").ObjectSerializerContext<string | null | T[keyof T]>} context context
+	 * @param {import("./ObjectMiddleware").ObjectSerializerContext<Array<string | null | T[keyof T]>>} context context
 	 */
 	serialize(obj, context) {
 		/** @type {Keys} */
@@ -28,7 +28,7 @@ class NullPrototypeObjectSerializer {
 	/**
 	 * Restores this instance from the provided deserializer context.
 	 * @template {object} T
-	 * @param {import("./ObjectMiddleware").ObjectDeserializerContext<string | null | T[keyof T]>} context context
+	 * @param {import("./ObjectMiddleware").ObjectDeserializerContext<Array<string | null | T[keyof T]>>} context context
 	 * @returns {T} null object
 	 */
 	deserialize(context) {
diff --git a/lib/serialization/ObjectMiddleware.js b/lib/serialization/ObjectMiddleware.js
index 3467625..665fb7a 100644
--- a/lib/serialization/ObjectMiddleware.js
+++ b/lib/serialization/ObjectMiddleware.js
@@ -60,12 +60,24 @@ Technically any value can be used.
 
 /** @typedef {EXPECTED_OBJECT | string} ReferenceableItem */
 
+/**
+ * First tuple element; element type for an open array; `EXPECTED_ANY` once exhausted.
+ * @template {readonly EXPECTED_ANY[]} T
+ * @typedef {T extends readonly [infer H, ...EXPECTED_ANY[]] ? H : T extends readonly [] ? EXPECTED_ANY : T extends readonly (infer E)[] ? E : never} Head
+ */
+
+/**
+ * Tuple without its first element; unchanged when `T` is an open array.
+ * @template {readonly EXPECTED_ANY[]} T
+ * @typedef {T extends readonly [EXPECTED_ANY, ...infer R] ? R : T} Tail
+ */
+
 /**
  * Defines the object serializer context type used by this module.
- * `T` is the union of value types this serializer writes.
- * @template [T=EXPECTED_ANY]
+ * `T` is the tuple of values written in order; each `write` consumes the head.
+ * @template {readonly EXPECTED_ANY[]} [T=readonly EXPECTED_ANY[]]
  * @typedef {object} ObjectSerializerContext
- * @property {(value: T) => void} write
+ * @property {(value: Head<T>) => ObjectSerializerContext<Tail<T>>} write
  * @property {(value: ReferenceableItem) => void} setCircularReference
  * @property {() => ObjectSerializerSnapshot} snapshot
  * @property {(snapshot: ObjectSerializerSnapshot) => void} rollback
@@ -75,10 +87,12 @@ Technically any value can be used.
 
 /**
  * Defines the object deserializer context type used by this module.
- * `T` is the union of value types this serializer reads back.
- * @template [T=EXPECTED_ANY]
+ * `T` is the tuple of values read in order: `read` returns the head, and `rest`
+ * re-types the same context to `Tail<T>` so successive reads stay positional.
+ * @template {readonly EXPECTED_ANY[]} [T=readonly EXPECTED_ANY[]]
  * @typedef {object} ObjectDeserializerContext
- * @property {() => T} read
+ * @property {() => Head<T>} read
+ * @property {ObjectDeserializerContext<Tail<T>>} rest
  * @property {(value: ReferenceableItem) => void} setCircularReference
  */
 
@@ -529,6 +543,7 @@ class ObjectMiddleware extends SerializerMiddleware {
 					}
 					throw err;
 				}
+				return ctx;
 			},
 			setCircularReference(ref) {
 				addReferenceable(ref);
@@ -746,6 +761,10 @@ class ObjectMiddleware extends SerializerMiddleware {
 			read() {
 				return /** @type {EXPECTED_ANY} */ (decodeValue());
 			},
+			// type-only cursor advance; same object, retyped to the tuple tail
+			get rest() {
+				return ctx;
+			},
 			setCircularReference(ref) {
 				addReferenceable(ref);
 			},
diff --git a/lib/serialization/PlainObjectSerializer.js b/lib/serialization/PlainObjectSerializer.js
index 19b5f36..fef4a38 100644
--- a/lib/serialization/PlainObjectSerializer.js
+++ b/lib/serialization/PlainObjectSerializer.js
@@ -75,7 +75,7 @@ class PlainObjectSerializer {
 	 * Serializes this instance into the provided serializer context.
 	 * @template {object} T
 	 * @param {T} obj plain object
-	 * @param {import("./ObjectMiddleware").ObjectSerializerContext<(keyof T)[] | keyof T | null | T[keyof T]>} context context
+	 * @param {import("./ObjectMiddleware").ObjectSerializerContext<Array<(keyof T)[] | keyof T | null | T[keyof T]>>} context context
 	 */
 	serialize(obj, context) {
 		const keys = /** @type {(keyof T)[]} */ (Object.keys(obj));
@@ -103,7 +103,7 @@ class PlainObjectSerializer {
 	/**
 	 * Restores this instance from the provided deserializer context.
 	 * @template {object} T
-	 * @param {import("./ObjectMiddleware").ObjectDeserializerContext<(keyof T)[] | keyof T | null | T[keyof T]>} context context
+	 * @param {import("./ObjectMiddleware").ObjectDeserializerContext<Array<(keyof T)[] | keyof T | null | T[keyof T]>>} context context
 	 * @returns {T} plain object
 	 */
 	deserialize(context) {
diff --git a/lib/serialization/RegExpObjectSerializer.js b/lib/serialization/RegExpObjectSerializer.js
index cbc0cca..d3516cf 100644
--- a/lib/serialization/RegExpObjectSerializer.js
+++ b/lib/serialization/RegExpObjectSerializer.js
@@ -4,8 +4,8 @@
 
 "use strict";
 
-/** @typedef {import("./ObjectMiddleware").ObjectDeserializerContext<string>} ObjectDeserializerContext */
-/** @typedef {import("./ObjectMiddleware").ObjectSerializerContext<string>} ObjectSerializerContext */
+/** @typedef {import("./ObjectMiddleware").ObjectDeserializerContext<[string, string]>} ObjectDeserializerContext */
+/** @typedef {import("./ObjectMiddleware").ObjectSerializerContext<[string, string]>} ObjectSerializerContext */
 
 class RegExpObjectSerializer {
 	/**
diff --git a/lib/serialization/SetObjectSerializer.js b/lib/serialization/SetObjectSerializer.js
index dc499ab..6db1ed2 100644
--- a/lib/serialization/SetObjectSerializer.js
+++ b/lib/serialization/SetObjectSerializer.js
@@ -9,7 +9,7 @@ class SetObjectSerializer {
 	 * Serializes this instance into the provided serializer context.
 	 * @template T
 	 * @param {Set<T>} obj set
-	 * @param {import("./ObjectMiddleware").ObjectSerializerContext<number | T>} context context
+	 * @param {import("./ObjectMiddleware").ObjectSerializerContext<Array<number | T>>} context context
 	 */
 	serialize(obj, context) {
 		context.write(obj.size);
@@ -21,7 +21,7 @@ class SetObjectSerializer {
 	/**
 	 * Restores this instance from the provided deserializer context.
 	 * @template T
-	 * @param {import("./ObjectMiddleware").ObjectDeserializerContext<number | T>} context context
+	 * @param {import("./ObjectMiddleware").ObjectDeserializerContext<Array<number | T>>} context context
 	 * @returns {Set<T>} date
 	 */
 	deserialize(context) {
diff --git a/lib/util/registerExternalSerializer.js b/lib/util/registerExternalSerializer.js
index 952faa4..36fcce8 100644
--- a/lib/util/registerExternalSerializer.js
+++ b/lib/util/registerExternalSerializer.js
@@ -234,7 +234,7 @@ register(
 		/**
 		 * Serializes this instance into the provided serializer context.
 		 * @param {SourceLocation} loc the location to be serialized
-		 * @param {import("../serialization/ObjectMiddleware").ObjectSerializerContext<number>} context context
+		 * @param {import("../serialization/ObjectMiddleware").ObjectSerializerContext<number[]>} context context
 		 * @returns {void}
 		 */
 		serialize(loc, { write }) {
@@ -246,7 +246,7 @@ register(
 
 		/**
 		 * Restores this instance from the provided deserializer context.
-		 * @param {import("../serialization/ObjectMiddleware").ObjectDeserializerContext<number>} context context
+		 * @param {import("../serialization/ObjectMiddleware").ObjectDeserializerContext<number[]>} context context
 		 * @returns {RealDependencyLocation} location
 		 */
 		deserialize({ read }) {
@@ -272,7 +272,7 @@ register(
 		/**
 		 * Serializes this instance into the provided serializer context.
 		 * @param {Position} pos the position to be serialized
-		 * @param {import("../serialization/ObjectMiddleware").ObjectSerializerContext<number>} context context
+		 * @param {import("../serialization/ObjectMiddleware").ObjectSerializerContext<number[]>} context context
 		 * @returns {void}
 		 */
 		serialize(pos, { write }) {
@@ -282,7 +282,7 @@ register(
 
 		/**
 		 * Restores this instance from the provided deserializer context.
-		 * @param {import("../serialization/ObjectMiddleware").ObjectDeserializerContext<number>} context context
+		 * @param {import("../serialization/ObjectMiddleware").ObjectDeserializerContext<number[]>} context context
 		 * @returns {SourcePosition} position
 		 */
 		deserialize({ read }) {
