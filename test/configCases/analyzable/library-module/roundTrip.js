"use strict";

// Marker: RoundTripConfigCases re-bundles this module library's output and asserts
// the analyzable `import("./dynamic.mjs")` survives being consumed by webpack again.
module.exports = true;
