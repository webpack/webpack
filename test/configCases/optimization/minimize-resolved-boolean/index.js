/* javascript minify marker */
it("should hand a minimizer plugin the boolean `optimization.minimize`", () => {
	// The minimizer plugin in webpack.config.js is the assertion.
	expect(true).toBe(true);
});
