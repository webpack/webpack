/* javascript minify excluded marker */
import "./page.html";
import "./style.css";

it("should minimize HTML alone, its inline CSS at the default options", () => {
	// The emitted files are the assertion — see test.config.js (afterExecute).
	expect(true).toBe(true);
});
