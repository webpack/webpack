/* javascript minify excluded marker */
it("should apply no minimizer when every applicable type is excluded", () => {
	// The emitted bundle is the assertion — see test.config.js (afterExecute).
	expect(true).toBe(true);
});
