"use strict";

module.exports = [
	// reported from both the pre-walk declarator and the call walk
	[{ message: /module\.createRequire supports only one argument/ }],
	[{ message: /module\.createRequire supports only one argument/ }],
	[{ message: /module\.createRequire failed parsing argument/ }],
	[{ message: /module\.createRequire failed parsing argument/ }]
];
