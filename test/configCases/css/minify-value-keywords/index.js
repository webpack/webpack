import "./style.css";

// Each of these is a value the minifier rewrites, so an entry nothing spells
// is a rewrite with no page behind it.
it("should rewrite every keyword the tables name", () => {
	expect(true).toBe(true);
});
