import "./style.css";

it("should drop an overridden declaration when the option asks", () => {
	// The emitted stylesheet is the assertion — see the snapshot in test.config.js.
	expect(true).toBe(true);
});
