import "./style.css";

it("should emit the stylesheet every `data:` payload was reached in", () => {
	// The assertions on the emitted file live in test.config.js (afterExecute).
	expect(true).toBe(true);
});
