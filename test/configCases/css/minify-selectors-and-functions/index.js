import "./style.css";

// Selectors and value functions the tables name, which a property-shaped
// fixture never reaches.
it("should read the selectors and functions the tables name", () => {
	expect(true).toBe(true);
});
