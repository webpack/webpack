import "./style.css";

// The at-rules and value functions the tables name, so a table entry with no
// page behind it is exercised here.
it("should read the at-rules and value functions the tables name", () => {
	expect(true).toBe(true);
});
