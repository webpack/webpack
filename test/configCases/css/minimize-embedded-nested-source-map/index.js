import "./style.css";

it("should emit a stylesheet whose map survived the nested rewrite", () => {
	// The assertions on the emitted map live in test.config.js (afterExecute).
	expect(true).toBe(true);
});
