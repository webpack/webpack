import "./style.css";

// Every property name the tables hold that no stylesheet in the corpus spells,
// so a wrong entry is a wrong declaration rather than a table nothing reads.
it("should read the property names no stylesheet spells", () => {
	expect(true).toBe(true);
});
