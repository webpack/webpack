import "./page.html";

it("should leave attribute values as written when both options are off", () => {
	// The emitted file is the assertion — see the snapshot in test.config.js.
	expect(true).toBe(true);
});
