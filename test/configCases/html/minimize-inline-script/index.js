it("should minify an inline script when optimization.minimize is enabled", () => {
	// The assertions on the emitted file live in test.config.js (afterExecute),
	// this keeps a runnable entry so the compilation produces a bundle.
	expect(true).toBe(true);
});
