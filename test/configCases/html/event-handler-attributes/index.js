import "./page.html";

// Every name in EVENT_HANDLER_ATTRIBUTES, each on an element the table allows
// it on, so a wrong binding shows as a handler that stopped being minified.
it("should minify every event handler attribute", () => {
	expect(true).toBe(true);
});
