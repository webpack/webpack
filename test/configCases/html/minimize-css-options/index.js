import "./page.html";

it("should hand `optimization.minimize.css` to the inline CSS the HTML carries", () => {
	// The emitted file is the assertion — see the snapshot in test.config.js.
	expect(true).toBe(true);
});
