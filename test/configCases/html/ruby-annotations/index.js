import "./page.html";

// `rb`, `rp`, `rt` and `rtc` each end on the next annotation or on `</ruby>`,
// so which end tags survive is what this pins.
it("should minify ruby annotations", () => {
	expect(true).toBe(true);
});
