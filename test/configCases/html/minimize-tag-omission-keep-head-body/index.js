import "./page.html";

it("should drop only the html tags when head and body are kept", () => {
	// The emitted file is the assertion — see the snapshot in test.config.js.
	expect(true).toBe(true);
});
