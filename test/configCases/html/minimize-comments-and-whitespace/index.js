import "./page.html";

it("should collapse smartly, keep a named comment and minify a conditional one", () => {
	// The emitted page is the assertion — see the snapshot in test.config.js.
	expect(true).toBe(true);
});
