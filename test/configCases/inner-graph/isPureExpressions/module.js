import { a } from "dep-a";
import { o } from "dep-o";
import { u } from "dep-u";
import { b } from "dep-b";
import { N } from "dep-n";

export const arr = [a, 1, 2];
export const obj = { key: o };
export const neg = -u;
export const sum = b + 1;
export const inst = /*#__PURE__*/ new N();
export const used = "hello";
