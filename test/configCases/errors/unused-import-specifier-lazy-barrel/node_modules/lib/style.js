import value from "./value/index.js";

export default function style() {
	const fn = () => 1;
	// the sole reference to `value` is dropped as dead code
	fn.propTypes = process.env.NODE_ENV !== "production" ? { value } : {};
	return fn;
}
