import value, { missing } from "./value/index.js";

export default function style() {
	const fn = () => 1;
	// Never read in production, so the reference is dropped as dead code.
	fn.propTypes = process.env.NODE_ENV !== "production" ? { value } : {};
	return fn;
}
