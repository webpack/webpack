"use strict";

/** @type {import("../../../../").Configuration} */
module.exports = {
	entry: "./index.js"
	// 'resolve.roots' defaults to the context, so a '/' request is looked up
	// there rather than at the file system root
};
