import { present } from "./named/index.js";
import { absent } from "./missing/index.js";

export default function style() {
	const fn = () => 1;
	fn.propTypes =
		process.env.NODE_ENV !== "production" ? { present, absent } : {};
	return fn;
}
