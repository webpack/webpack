"use strict";

// the library wrappers need a host (a `define`, a `System`, a jsonp callback),
// which each library type's own test cases cover — this one is about which
// externals the wrapper still references
module.exports = {
	noTests: true
};
