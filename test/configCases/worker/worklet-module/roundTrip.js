"use strict";

// Marker: RoundTripConfigCases re-bundles this case's output and asserts the
// analyzable `new URL("./<chunk>.mjs", import.meta.url)` worklet reference survives.
module.exports = true;
